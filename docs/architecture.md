# Architecture

UI → Core → Engines → Services → Database.

الـSkills منفصلة عن الـEngines. الـEngine ينفذ، والـSkill تعلمه كيف يكتب/يراجع.

مسار الفيديو:
Input → Research → Knowledge → Strategy → Story → Hook → Script → Humanize → Review → Final → Production.

مسار PDF:
Upload → Page Extraction → Page-aware Source → Input Router → Knowledge/Explanation.

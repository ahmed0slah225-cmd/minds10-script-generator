# Remaining product gaps

The core platform is in place. Further hardening should add: fully normalized persistent tables and stage-level version rows, per-engine model overrides, remote model availability checks, cache keys, token/cost accounting, richer PDF page indexing, deterministic claim extraction, and automated post-edit re-review loops.

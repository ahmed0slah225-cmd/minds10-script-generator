import time
import re
import streamlit as st
from google import genai

# ============================================================
# إعداد الصفحة
# ============================================================

st.set_page_config(
    page_title="غرفة كتابة يوتيوب - 10 عقول",
    page_icon="🧠",
    layout="wide",
)

st.title("🧠 غرفة كتابة سكريبت يوتيوب - نظام 10 عقول")
st.write(
    "نظام كتابة متعدد المراحل: بحث وفهم للمشاهد ← زاوية جديدة ← هوك ← رحلة سردية "
    "← قصص وأمثلة ← هندسة الاستبقاء ← مسودة كاملة ← Humanization ← نقد قاسٍ "
    "← إعادة كتابة نهائية جاهزة للتسجيل."
)

# ============================================================
# الإعدادات
# ============================================================

with st.sidebar:
    st.header("⚙️ الإعدادات")

    api_key = st.text_input(
        "Google Gemini API Key",
        type="password",
    )

    model_name = st.text_input(
        "اسم الموديل",
        value="gemini-3.6-flash",
    )

    fallback_model = st.text_input(
        "Fallback Model",
        value="gemini-3.6-flash",
    )

    target_minutes = st.slider(
        "مدة الفيديو المستهدفة بالدقائق",
        min_value=10,
        max_value=60,
        value=30,
        step=5,
    )

    audience = st.text_area(
        "الجمهور المستهدف",
        value=(
            "شباب وبنات من 18 إلى 35 سنة، عايزين يفهموا نفسهم ويحسنوا حياتهم، "
            "لكن بيكرهوا الكلام الوعظي والتنمية البشرية المكررة، وبيخرجوا بسرعة "
            "لو حسوا إن الفيديو بيحاضر عليهم أو بيقول كلام عام."
        ),
        height=130,
    )

    sources = st.text_area(
        "مصادر أو كتب أو ملاحظات اختيارية",
        placeholder="اكتب هنا كتاب، بحث، ملاحظات، أفكار، أو أي مادة عايز السكريبت يعتمد عليها.",
        height=140,
    )

    st.divider()

    show_all_outputs = st.checkbox(
        "عرض مخرجات كل العقول",
        value=True,
    )


# ============================================================
# القواعد العامة
# ============================================================

GLOBAL_RULES = """
أنت عضو داخل غرفة كتابة حقيقية لفيديو يوتيوب طويل.

الهدف النهائي ليس إنتاج مقال، ولا تلخيص كتاب، ولا محاضرة في علم النفس.
الهدف هو بناء تجربة مشاهدة تجعل شخصًا حقيقيًا يشعر في أول لحظات أن:
"الفيديو ده بيتكلم عن حاجة أنا عايشها فعلاً"
ثم يكمل لأنه يريد أن يعرف كيف تتكشف الفكرة.

اكتب بالعربية العامية المصرية الطبيعية المناسبة للكلام أمام الكاميرا.

القواعد غير القابلة للكسر:

1. لا تبدأ بتحية.
2. لا تبدأ بتعريف أكاديمي.
3. ممنوع: "في الفيديو ده هنتكلم عن".
4. ممنوع: "متعرفش إن..." و"هل تعلم أن..." و"انت نايم على..." وأي كليشيه مشابه.
5. ممنوع استخدام "استنى للنهاية" كحيلة للاستبقاء.
6. لا تخترع دراسة أو رقمًا أو تجربة أو اسم عالم.
7. لو معلومة غير مؤكدة، علّم عليها بوضوح.
8. لا تستخدم مصطلحًا علميًا إلا لو سيساعد فعلًا في تفسير تجربة إنسانية.
9. ابدأ بما يعيشه الإنسان، ثم فسّر لماذا قد يحدث.
10. لا تكرر نفس الفكرة بصياغتين مختلفتين لمجرد إطالة النص.
11. لا تجعل النص كله جملًا قصيرة؛ التنوع الطبيعي في طول الجمل أهم.
12. لا تجعل كل 15 ثانية سؤالًا أو مفاجأة مصطنعة.
13. المقصود بالاستبقاء: تغيير حقيقي في الحالة، مثل:
    موقف، اعتراف، اعتراض، اكتشاف، قصة، نتيجة، مثال، تغيير منظور، إجابة،
    مفارقة، أو فتح سؤال حقيقي له إجابة لاحقة.
14. كل جزء من الفيديو يجب أن يدفع المشاهد للأمام.
15. لا تعامل المشاهد كأنه غبي، ولا تحاضر عليه.
16. لا تستخدم نبرة "أنا هصلح حياتك".
17. لا تقل للمشاهد إنه لازم يعمل شيئًا إلا عندما يكون التطبيق نتيجة طبيعية لما اكتشفه.
18. لا تستخدم لغة ذكاء اصطناعي أو لغة مقالات مثل:
    "في عالم يتسم بـ"، "من الجدير بالذكر"، "دعنا نتعمق"،
    "هذا يقودنا إلى"، "الأمر الذي يجعلنا".
19. الكلام لازم يتقال بصوت إنسان، مش يتقري من مقال.
20. لو الموضوع يستحق 18 دقيقة فقط، لا تحشوه إلى 30 دقيقة.
21. أي تشبيه هو وسيلة للفهم، وليس حقيقة علمية.
22. لا تكتب سكريبتًا يشبه سلسلة نصائح منفصلة.
23. الفيديو يجب أن يكون رحلة اكتشاف واحدة لها بداية وتحول ونهاية.
"""


COMMON_INPUTS = """
================ بيانات المشروع ================

فكرة أو عنوان الفيديو:
{topic}

الجمهور المستهدف:
{audience}

المدة المستهدفة:
{target_minutes} دقيقة

مصادر أو كتب أو ملاحظات المستخدم:
{sources}

=================================================
"""


# ============================================================
# العقول العشرة
# ============================================================

MINDS = [

    # --------------------------------------------------------
    # العقل 1
    # --------------------------------------------------------
    {
        "id": 1,
        "name": "العقل 1 — الباحث ومفكك المشكلة",
        "role": "Researcher + Problem Deconstructor",
        "description": 'أنت العقل الأول: الباحث ومفكك المشكلة. مهمتك استخراج السؤال الحقيقي تحت عنوان الفيديو، وليس كتابة السكريبت.\n\nحلل الموضوع إلى طبقات: المشكلة التي يقولها المشاهد، ما يشعر به، ما يفعله، التفسير الذي يصدقه عن نفسه، الآلية الأعمق المحتملة، والنتيجة الظاهرة في حياته. اكتب 5 صيغ مختلفة للسؤال المركزي ثم اختر الأقوى، واشرح لماذا يصلح لحمل فيديو طويل.\n\nاكتب 10 مواقف يومية مصرية تظهر فيها المشكلة، و15 سؤالاً داخلياً قد يفكر فيها المشاهد ولا يقولها، و10 معتقدات شائعة عن الموضوع مع تصنيف كل واحد: صحيح جزئياً، ناقص، أو مضلل. اشرح لماذا يبدو كل معتقد مقنعاً.\n\nاستخرج حتى 10 مفاهيم أو أفكار بحثية تخدم الموضوع، لكن لا تستخدم إلا المعلومات الموجودة في المصادر المرفقة أو المعلومات التي تستطيع تحديدها بثقة. لكل فكرة اكتب: الفكرة، ما الذي تفسره، درجة الثقة، ومخاطر تبسيطها. لا تخترع دراسة أو رقماً أو اقتباساً أو اسماً. أي نقطة تحتاج مراجعة علّم عليها [تحتاج تحقق].\n\nاكتب 7 مفارقات تصلح لبناء الفضول، ثم «الخيط الأحمر» للفيديو في 5 جمل من السؤال إلى الكشف إلى الحل. اختم بتعريف إنساني للمشكلة، لا تعريف أكاديمي، وبقائمة المعلومات التي لا يجوز استخدامها قبل التحقق. ممنوع كتابة الهوك أو المقدمة أو السكريبت النهائي.',
    },

    # --------------------------------------------------------
    # العقل 2
    # --------------------------------------------------------
    {
        "id": 2,
        "name": "العقل 2 — محلل نفسية المشاهد",
        "role": "Viewer Psychologist",
        "description": 'أنت العقل الثاني: محلل نفسية المشاهد وصوته الداخلي. مهمتك أن تفهم الشخص الذي سيضغط على الفيديو، من غير تشخيص طبي أو تعميم جارح.\n\nاكتب بروفايل إنسانياً لمشاهد أساسي: يومه، لحظة بحثه عن الموضوع، ما يقوله لنفسه، ما يلوم نفسه عليه، وما يخاف أن يكون صحيحاً عنه. أنشئ 5 أنواع فرعية من المشاهدين، و20 اعتراضاً قد يقولها المشاهد أثناء الفيديو، ثم اكتب أمام كل اعتراض رداً تحريرياً يبدأ بالاعتراف بالمشاعر قبل التوضيح.\n\nاكتب 15 جملة اعتراف داخلية بالعامية المصرية تجعل المشاهد يقول «دي أنا»، على أن تكون محددة ومبنية على مواقف، لا شعارات. حدد ما لا يريد المشاهد سماعه، وما يحتاج سماعه بصدق، وأعمق ألم، وأعمق خوف، وأمل واقعي، وسوء الفهم الأساسي عن نفسه.\n\nابنِ خريطة لحظة الضغط على الفيديو: ماذا يجب أن يشعر بعد 5 ثوانٍ، وما السؤال الذي يدخل دماغه بعد 15 ثانية، وما الوعد الفكري بعد 30 ثانية، ولماذا سيكمل بعد دقيقتين وخمس دقائق. لا تكتب السكريبت أو الهوك النهائي.',
    },

    # --------------------------------------------------------
    # العقل 3
    # --------------------------------------------------------
    {
        "id": 3,
        "name": "العقل 3 — صائد الزاوية والاختراق",
        "role": "Big Idea + Unique Angle Strategist",
        "description": 'أنت العقل الثالث: صائد الزاوية والاختراق. مهمتك العثور على الفكرة التي تجعل الفيديو ضرورياً، وليس إنتاج نصائح عامة.\n\nاقترح 7 زوايا مختلفة للموضوع. لكل زاوية اكتب: الفكرة الكبيرة، الاعتقاد القديم، الاكتشاف الجديد، لماذا يهتم المشاهد، ما خطر المبالغة، وكيف تتحول إلى رحلة طويلة. اقتل الزوايا المكررة واختر زاوية واحدة حادة لكن منصفة.\n\nاكتب التحول المركزي بهذه الصيغة: «في البداية المشاهد يعتقد أن __، وفي النهاية يفهم أن __». ثم حدد السؤال المركزي، الصراع المركزي، الوعد الحقيقي، الشيء الذي يجب تأجيل كشفه، و3 أفكار فرعية تخدم الزاوية دون تشتيت.\n\nاختبر الزاوية ضد الوعظ والتنمية البشرية المكررة. لو احتاجت ادعاءً غير مؤكد، ضع [تحتاج تحقق]. لا تكتب الهوك النهائي ولا السكريبت.',
    },

    # --------------------------------------------------------
    # العقل 4
    # --------------------------------------------------------
    {
        "id": 4,
        "name": "العقل 4 — مهندس الهوك والبداية",
        "role": "Hook + Opening Specialist",
        "description": 'أنت العقل الرابع: مهندس الهوك والبداية. مهمتك بناء أول 90 ثانية بحيث يدخل المشاهد في موقف أو سؤال حقيقي ويشعر أن هناك شيئاً يخصه شخصياً.\n\nاستخرج من المخرجات: أقوى موقف، أقوى تناقض، الاعتقاد الذي سيُقلب، والسؤال المخفي. اكتب 12 افتتاحية مختلفة بالعامية المصرية، بالتنويع بين مشهد يومي، اعتراف، حوار داخلي، بداية قصة، ومفارقة. لكل افتتاحية اكتب أول جملة، الجملة التي توسع التوتر، السؤال المعلق، ما الذي لن تكشفه بعد، والانتقال للفصل الأول.\n\nقيّم كل افتتاحية من 10 في الصدق والفضول وقابلية النطق والقوة العاطفية. اختر أفضل 3، ثم اكتب كل واحدة حتى 90 ثانية وفق المسار: هوك خاطف، موقف واقعي، سؤال معلق، تناقض، وعد صادق بالرحلة. ممنوع التحيات الطويلة، وممنوع عبارات «استنى للنهاية»، «هقولك السر»، «في الفيديو ده هنتكلم»، أو الصدمة المصطنعة.',
    },

    # --------------------------------------------------------
    # العقل 5
    # --------------------------------------------------------
    {
        "id": 5,
        "name": "العقل 5 — مهندس الرحلة السردية",
        "role": "Narrative Architect",
        "description": 'أنت العقل الخامس: مهندس الرحلة السردية. لا تكتب السكريبت؛ ابنِ رحلة اكتشاف متصلة.\n\nحوّل الزاوية والهوك إلى 7 أو 9 فصول تناسب مدة الفيديو. ابدأ من نقطة وقوف المشاهد، ثم أول كسر لتفسيره القديم، ثم الاكتشاف الأول، ثم التعقيد الذي يثبت أن الإجابة الأولى غير كافية، ثم التحول، ثم التطبيق، ثم العودة إلى موقف البداية بوعي مختلف.\n\nلكل فصل اكتب: الاسم الجذاب، الزمن التقريبي، الهدف الدرامي، السؤال، ما يعرفه المشاهد، ما سيكتشفه، نوع المادة، المصدر المطلوب، المثال المناسب، الـ Open Loop، الـ Payoff، مستوى الطاقة، وطريقة الانتقال بدون تكرار «لكن في حاجة أهم».\n\nوزّع الكشف على الرحلة ولا تضع الحل كاملاً في البداية. أنشئ جدولاً زمنياً، وحدد 8 أماكن لإعادة جذب المشاهد، و3 أماكن للتهدئة والتأمل. لا تكتب الفقرات النهائية.',
    },

    # --------------------------------------------------------
    # العقل 6
    # --------------------------------------------------------
    {
        "id": 6,
        "name": "العقل 6 — راوي القصص ومترجم الأفكار للحياة",
        "role": "Storyteller + Human Examples",
        "description": 'أنت العقل السادس: راوي القصص ومترجم الأفكار للحياة. مهمتك إعطاء الهيكل لحماً ودماً، لا كتابة السكريبت الكامل.\n\nلكل فصل اقترح 3 مشاهد يومية مصرية بتفاصيل حسية ضرورية: منبه، موبايل، مواصلات، قعدة أصحاب، لحظة قبل النوم، أو يوم ضاع بدون ما الشخص يحس. اكتب ما يراه الشخص، ماذا يقول لنفسه، الصراع الداخلي، وما الذي يكشفه المشهد.\n\nاقترح قصصاً أو تجارب من المصادر المرفقة فقط، ولا تنسب قصة لشخص أو كتاب بدون تحقق. لو كانت القصة مركبة، اكتب قبلها [مشهد تمثيلي مركب]. اقترح 10 تشبيهات أصلية تشرح المفاهيم النفسية أو السلوكية بدون تحويل التشبيه إلى حقيقة علمية.\n\nبعد كل مشهد أو قصة اكتب جسر الانتقال إلى الشرح. رتّب بنك المشاهد حسب الفصول، واحذف أي مثال يؤدي نفس وظيفة مثال آخر. لا تكتب السكريبت النهائي.',
    },

    # --------------------------------------------------------
    # العقل 7
    # --------------------------------------------------------
    {
        "id": 7,
        "name": "العقل 7 — مهندس الاستبقاء والإيقاع",
        "role": "Retention + Rhythm Architect",
        "description": 'أنت العقل السابع: مهندس الاستبقاء والإيقاع. مهمتك حماية الرحلة من الملل، وليس وضع سؤال أو مفاجأة مصطنعة كل 15 ثانية.\n\nقسّم الهيكل إلى كتل من 30 إلى 60 ثانية، وداخل كل كتلة حدد: حالة المشاهد، ما يعرفه، ما يشعر به، التغيير الحقيقي، نوع المادة، والجملة أو الصورة التي تعيد انتباهه. كل 2 إلى 4 دقائق يجب أن يحدث كشف أو إعادة تفسير أو مثال يغير زاوية النظر.\n\nطبّق اختبار الخسارة: لو أغلق المشاهد الفيديو هنا، ماذا خسر؟ وطبّق اختبار الشرح: لا تسمح بأكثر من 40 ثانية شرح مجرد بدون مثال أو قصة أو تشبيه. وطبّق اختبار التكرار: احذف أي فقرة تعيد فكرة سابقة.\n\nأخرج خريطة إيقاع كاملة، وأخطر 15 مكاناً قد يمل فيها المشاهد مع علاج عملي، وقائمة Pattern Breaks طبيعية مثل تغيير المشهد أو اعتراف أو اعتراض أو صمت، وليس عبارات تسويقية. لا تكتب السكريبت النهائي.',
    },

    # --------------------------------------------------------
    # العقل 8
    # --------------------------------------------------------
    {
        "id": 8,
        "name": "العقل 8 — الكاتب الرئيسي للمسودة",
        "role": "Long-form YouTube Script Writer",
        "description": 'أنت العقل الثامن: الكاتب الرئيسي للمسودة الكاملة. كل ما وصلك مواد خام؛ لا تلصق النتائج وراء بعضها.\n\nاكتب سكريبت فيديو كامل بالعامية المصرية الطبيعية، جاهز للتسجيل، بمدة قريبة من المدة المطلوبة. لو الموضوع لا يحتمل المدة، لا تحشُه. ابدأ بالهوك المختار فوراً، ثم اترك السؤال يتطور خلال رحلة واحدة متصلة.\n\nادمج الأمثلة والقصص والتشبيهات داخل الشرح، واجعل كل معلومة تظهر لأنها تجيب سؤالاً أو تفتح اكتشافاً. لا تجعل النص سلسلة نصائح. اجعل التطبيق نتيجة طبيعية للتحول الفكري، واذكر العقبات والرجوع للخلف.\n\nاستخدم تنوعاً حقيقياً في طول الجمل، وأسئلة قليلة في أماكنها، واعترافات حقيقية فقط إن كانت متاحة. ممنوع اللغة الأكاديمية أو لغة الذكاء الاصطناعي. اكتب [وقفة] أو [بهدوء] أو [لقطة موبايل] عند الحاجة فقط. أخرج السكريبت كاملاً، ولا تضع تحليلاً قبله.',
    },

    # --------------------------------------------------------
    # العقل 9
    # --------------------------------------------------------
    {
        "id": 9,
        "name": "العقل 9 — المحرر البشري القاسي",
        "role": "Humanization Editor + Brutal Script Doctor",
        "description": 'أنت العقل التاسع: المحرر البشري القاسي وطبيب السكريبت. أمامك المسودة الكاملة، ولا تجامل.\n\nراجع أول 5 و15 و60 ثانية، واكتب هل يبدأ الفيديو من مشكلة حقيقية أم مقدمة طويلة. علّم كل جملة تشبه مقالاً أو ترجمة أو كلاماً آلياً، واقترح بديلها بلسان مصري منطوق. احذف التكرار والحشو والأحكام الجاهزة والتفكير الإيجابي المزيف.\n\nاختبر هل تم التعامل مع اعتراضات المشاهد، وهل هناك أكثر من 40 ثانية شرح بلا مثال، وهل يحدث كشف جديد كل 2 إلى 4 دقائق، وهل الحل واقعي. اكتب تقريراً به: ما يعمل، أخطر 20 مشكلة، الجملة أو الفقرة المتسببة في كل مشكلة، البديل المقترح، الكلمات التي يجب حذفها، اللحظات التي يجب تعميقها، وخطة تعديل مرتبة للعقل العاشر.\n\nلا تعيد كتابة السكريبت كاملاً. ركز على تعديلات محددة، وعلّم الادعاءات التي تحتاج تحققاً.',
    },

    # --------------------------------------------------------
    # العقل 10
    # --------------------------------------------------------
    {
        "id": 10,
        "name": "العقل 10 — الكاتب النهائي والمخرج الداخلي",
        "role": "Final Master Writer + Viewer Advocate",
        "description": 'أنت العقل العاشر: الكاتب النهائي والمخرج الداخلي. أمامك كل التحليلات، الخطة، المسودة، ونقد المحرر. أعد بناء النص كأنه كتبه إنسان واحد بصوت واحد.\n\nاكتب النسخة النهائية كاملة بالعامية المصرية الحية، بدون مقدمة من نوع «إليك السكريبت». ابدأ بالهوك، وحافظ على رحلة واحدة: سؤال، موقف، تفسير أول، كسر، بحث أو مفهوم، مثال، اعتراف، قلب الفكرة، تطبيق متدرج، وعودة لمشهد البداية بنظرة مختلفة.\n\nاحذف أي تكرار حتى لو كان جميلاً، ولا تضف طولاً بلا وظيفة. حافظ على الدقة: لا تخترع رقماً أو دراسة أو اقتباساً أو تجربة. أي ادعاء يحتاج مراجعة علّمه بوضوح. استخدم [B-ROLL] و[لقطة] و[وقفة] فقط عندما تخدم الأداء.\n\nفي أول 15 ثانية يجب أن يكون هناك سؤال أو موقف أو مفارقة حقيقية. خلال الدقيقة الأولى يجب أن يفهم المشاهد لماذا الموضوع يخصه، لكن لا تكشف كل الإجابة. حافظ على تنوع الإيقاع، وعلى جمل قابلة للنطق، وعلى نبرة قريبة لا تعظ المشاهد.\n\nبعد نهاية السكريبت أضف فقط:\n==============================\nملاحظات تحقق قبل النشر\n==============================\nاذكر الادعاءات التي تحتاج مراجعة، أو اكتب: «لا توجد ادعاءات محددة تحتاج تحقق إضافي.»',
    },
]


# ============================================================
# تمرير السياق بين العقول
# ============================================================

ROUTES = {
    1: [],
    2: [1],
    3: [1, 2],
    4: [2, 3],
    5: [1, 2, 3, 4],
    6: [1, 2, 3, 5],
    7: [3, 4, 5, 6],
    8: [1, 2, 3, 4, 5, 6, 7],
    9: [2, 3, 4, 7, 8],
    10: [1, 2, 3, 4, 5, 6, 7, 8, 9],
}


# ============================================================
# أدوات مساعدة
# ============================================================

def trim_text(text, max_chars=12000):
    """
    تقليل حجم مخرجات العقول القديمة قبل تمريرها للعقل التالي.
    نحتفظ بالبداية والنهاية حتى لا يضيع القرار أو الخلاصة.
    """
    if not text:
        return ""

    if len(text) <= max_chars:
        return text

    head_size = int(max_chars * 0.65)
    tail_size = max_chars - head_size

    return (
        text[:head_size]
        + "\n\n"
        + "[... تم اختصار جزء من المخرج للحفاظ على حجم السياق ...]"
        + "\n\n"
        + text[-tail_size:]
    )


def format_context(selected_outputs):
    if not selected_outputs:
        return "لا توجد مخرجات سابقة مرتبطة بهذه المهمة."

    parts = []

    for label, value in selected_outputs.items():
        parts.append(
            f"""
================ {label} ================

{trim_text(value)}

================ نهاية {label} ================
"""
        )

    return "\n".join(parts)


def extract_retry_delay(error_text, default_delay):
    """
    يحاول استخراج مدة الانتظار لو الـ API كتب:
    retry in 40.18s
    """
    match = re.search(
        r"retry(?:\s+in)?\s+([0-9]+(?:\.[0-9]+)?)\s*s",
        error_text,
        flags=re.IGNORECASE,
    )

    if match:
        try:
            return max(1, float(match.group(1)))
        except ValueError:
            pass

    return default_delay


def ask_gemini(
    client,
    prompt,
    requested_model,
    fallback_model,
    max_retries=6,
    base_delay=5,
    status_slot=None,
):
    """
    استدعاء Gemini مع:
    - إعادة محاولة عند 429 / 503.
    - احترام مدة Retry لو ظهرت داخل رسالة الخطأ.
    - Fallback لو الموديل غير موجود.
    """

    current_model = (
        requested_model.strip()
        if requested_model and requested_model.strip()
        else fallback_model.strip()
    )

    fallback = (
        fallback_model.strip()
        if fallback_model and fallback_model.strip()
        else current_model
    )

    last_exc = None

    for attempt in range(1, max_retries + 1):

        try:
            response = client.models.generate_content(
                model=current_model,
                contents=prompt,
            )

            text = getattr(response, "text", "") or ""

            if not text.strip():
                raise RuntimeError("تم استلام رد فارغ من الموديل.")

            return text

        except Exception as exc:

            last_exc = exc

            error_text = str(exc)
            error_lower = error_text.lower()

            not_found = (
                "404" in error_text
                or "not_found" in error_lower
                or "not found" in error_lower
                or "no longer available" in error_lower
            )

            overloaded = (
                "429" in error_text
                or "503" in error_text
                or "resource_exhausted" in error_lower
                or "unavailable" in error_lower
                or "overloaded" in error_lower
                or "high demand" in error_lower
                or "quota" in error_lower
            )

            # ------------------------------------------------
            # لو الموديل غير موجود
            # ------------------------------------------------
            if not_found and current_model != fallback:

                if status_slot is not None:
                    status_slot.warning(
                        f"⚠️ الموديل '{current_model}' غير متاح. "
                        f"جاري التجربة بالموديل البديل '{fallback}'."
                    )

                current_model = fallback
                continue

            # ------------------------------------------------
            # لو حصل Rate Limit أو ضغط
            # ------------------------------------------------
            if overloaded and attempt < max_retries:

                exponential_delay = base_delay * (2 ** (attempt - 1))

                retry_delay = extract_retry_delay(
                    error_text,
                    exponential_delay,
                )

                wait_time = max(retry_delay, exponential_delay)

                # سقف حتى لا ينتظر المستخدم وقتًا غير منطقي
                wait_time = min(wait_time, 120)

                if status_slot is not None:
                    status_slot.info(
                        f"⏳ Gemini طلب الانتظار بسبب ضغط أو حد استخدام. "
                        f"المحاولة {attempt}/{max_retries}. "
                        f"الانتظار حوالي {int(wait_time)} ثانية..."
                    )

                time.sleep(wait_time)
                continue

            raise

    raise last_exc


def build_prompt(
    mind,
    topic,
    audience_text,
    source_text,
    target_minutes,
    outputs,
):

    route_ids = ROUTES.get(mind["id"], [])

    selected_outputs = {}

    for mind_id in route_ids:
        key = f"العقل {mind_id}"

        if key in outputs and outputs[key].strip():
            selected_outputs[key] = outputs[key]

    project_context = COMMON_INPUTS.format(
        topic=topic,
        audience=audience_text,
        target_minutes=target_minutes,
        sources=source_text.strip()
        if source_text and source_text.strip()
        else "لا توجد مصادر إضافية قدمها المستخدم.",
    )

    return f"""
{GLOBAL_RULES}

{project_context}

================================================
اسم العقل:
{mind["name"]}

التخصص:
{mind["role"]}
================================================

تعليماتك الخاصة:

{mind["description"]}

================================================
مخرجات العقول السابقة المرتبطة بمهمتك
================================================

{format_context(selected_outputs)}

================================================
طريقة التعامل مع المخرجات السابقة
================================================

1. لا تعتبر أي مخرج سابق حقيقة مقدسة.
2. لو وجدت تكرارًا، لا تكرره.
3. لو وجدت تناقضًا، اختر القرار الأفضل واذكر السبب إذا كانت مهمتك تحتاج ذلك.
4. لا تحاول إظهار أنك قرأت كل شيء بتلخيصه مرة أخرى.
5. أضف قيمة جديدة واضحة.
6. لا تكتب كلامًا تنظيريًا للعقل التالي.
7. المخرج يجب أن يكون عمليًا وقابلًا للاستخدام.
8. التزم بمهمة عقلك فقط.
"""


# ============================================================
# Session State
# ============================================================

if "pipeline_outputs" not in st.session_state:
    st.session_state.pipeline_outputs = {}

if "pipeline_topic" not in st.session_state:
    st.session_state.pipeline_topic = ""

if "last_error" not in st.session_state:
    st.session_state.last_error = ""


# ============================================================
# إدخال الموضوع
# ============================================================

topic = st.text_area(
    "🎯 اكتب فكرة أو عنوان الفيديو",
    height=150,
    placeholder=(
        "مثال: ليه كل مرة ببدأ عادة جديدة وبعدين برجع لنقطة الصفر؟\n"
        "أو: الثقة بالنفس... ليه في ناس شكلهم واثقين رغم إنهم خايفين؟"
    ),
)


# ============================================================
# أزرار التحكم
# ============================================================

has_progress = len(st.session_state.pipeline_outputs) > 0

if has_progress:
    run_label = "▶️ كمّل من آخر عقل وقفنا عنده"
else:
    run_label = "🚀 شغّل نظام الـ10 عقول"

col_run, col_reset = st.columns([3, 1])

with col_run:
    run_button = st.button(
        run_label,
        type="primary",
        use_container_width=True,
    )

with col_reset:
    reset_button = st.button(
        "🔄 ابدأ من جديد",
        use_container_width=True,
    )


if reset_button:

    st.session_state.pipeline_outputs = {}
    st.session_state.pipeline_topic = ""
    st.session_state.last_error = ""

    st.rerun()


if has_progress:

    completed = len(st.session_state.pipeline_outputs)

    st.info(
        f"📌 تم حفظ {completed} من {len(MINDS)} عقول. "
        "لو حصل Rate Limit أو خطأ، تقدر تكمل بدون إعادة العقول اللي خلصت."
    )


# ============================================================
# التشغيل
# ============================================================

if run_button:

    if not api_key.strip():
        st.error("❌ اكتب Gemini API Key الأول.")
        st.stop()

    if not topic.strip():
        st.warning("⚠️ اكتب فكرة الفيديو أو العنوان الأول.")
        st.stop()

    # لو المستخدم غيّر الموضوع، امسح التقدم القديم
    if (
        st.session_state.pipeline_topic
        and st.session_state.pipeline_topic != topic.strip()
    ):

        st.warning(
            "⚠️ تم تغيير موضوع الفيديو، لذلك سيتم بدء Pipeline جديد."
        )

        st.session_state.pipeline_outputs = {}

    st.session_state.pipeline_topic = topic.strip()
    st.session_state.last_error = ""

    try:

        client = genai.Client(
            api_key=api_key.strip()
        )

        outputs = st.session_state.pipeline_outputs

        total_minds = len(MINDS)
        completed_count = len(outputs)

        progress = st.progress(
            completed_count / total_minds
            if completed_count
            else 0
        )

        status = st.empty()

        st.divider()
        st.subheader("🧠 غرفة الكتابة")

        st.caption(
            "كل عقل يستلم فقط السياق الذي يحتاجه. "
            "العقل 8 يكتب المسودة الكاملة، العقل 9 ينتقدها، "
            "والعقل 10 يعيد كتابتها كنسخة نهائية."
        )

        for index, mind in enumerate(MINDS):

            mind_key = f"العقل {mind['id']}"

            # --------------------------------------------
            # تخطي العقل المكتمل
            # --------------------------------------------
            if mind_key in outputs:

                if show_all_outputs:

                    with st.expander(
                        f"✅ {mind['name']} — محفوظ",
                        expanded=False,
                    ):
                        st.markdown(outputs[mind_key])

                progress.progress(
                    (index + 1) / total_minds
                )

                continue

            # --------------------------------------------
            # تشغيل العقل الحالي
            # --------------------------------------------
            status.info(
                f"🧠 العقل {mind['id']}/{total_minds} يعمل الآن: "
                f"{mind['name']}"
            )

            prompt = build_prompt(
                mind=mind,
                topic=topic.strip(),
                audience_text=audience,
                source_text=sources,
                target_minutes=target_minutes,
                outputs=outputs,
            )

            result = ask_gemini(
                client=client,
                prompt=prompt,
                requested_model=model_name,
                fallback_model=fallback_model,
                status_slot=status,
            )

            if not result.strip():
                raise RuntimeError(
                    f"العقل {mind['id']} أعاد نتيجة فارغة."
                )

            # --------------------------------------------
            # حفظ مباشر
            # --------------------------------------------
            outputs[mind_key] = result

            st.session_state.pipeline_outputs = outputs

            if show_all_outputs:

                with st.expander(
                    f"🧠 {mind['name']}",
                    expanded=(mind["id"] >= 8),
                ):
                    st.markdown(result)

            progress.progress(
                (index + 1) / total_minds
            )

        # ====================================================
        # السكريبت النهائي
        # ====================================================

        final_script = outputs.get(
            "العقل 10",
            ""
        )

        status.success(
            "🎉 اكتمل نظام الـ10 عقول والسكريبت النهائي جاهز."
        )

        st.divider()

        st.header("🎬 MASTER SCRIPT")

        st.text_area(
            "📜 النسخة النهائية",
            value=final_script,
            height=1000,
        )

        st.download_button(
            label="⬇️ تحميل السكريبت النهائي TXT",
            data=final_script,
            file_name="master_youtube_script.txt",
            mime="text/plain",
            use_container_width=True,
        )

        # ====================================================
        # تحميل جميع المخرجات
        # ====================================================

        all_outputs_text = []

        for mind in MINDS:

            key = f"العقل {mind['id']}"

            if key in outputs:

                all_outputs_text.append(
                    f"""
====================================================
{mind["name"]}
====================================================

{outputs[key]}
"""
                )

        st.download_button(
            label="⬇️ تحميل تقرير جميع العقول بالكامل TXT",
            data="\n\n".join(all_outputs_text),
            file_name="all_minds_report.txt",
            mime="text/plain",
            use_container_width=True,
        )

    except Exception as e:
        st.session_state.last_error = str(e)
        st.error(f"❌ حدث خطأ أثناء المعالجة: {str(e)}")

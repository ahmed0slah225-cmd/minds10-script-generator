import streamlit as st
import google.generativeai as genai
import time

# ============================================================
# إعدادات الصفحة والستايل
# ============================================================
st.set_page_config(page_title="Writer Room - 10 Minds", layout="wide")

st.markdown("""
    <style>
    .stProgress > div > div > div > div { background-color: #00ff00; }
    .mind-card { border: 1px solid #ddd; padding: 15px; border-radius: 10px; margin-bottom: 10px; background-color: #f9f9f9; }
    </style>
    """, unsafe_allow_html=True)

st.title("🎬 غرفة صناعة المحتوى الإبداعي (الـ 10 عقول)")
st.write("النظام ده بيحول فكرتك لسكريبت فيديو 30 دقيقة بأسلوب مصري بشري 'خاطف'.")

# ============================================================
# الإعدادات الجانبية
# ============================================================
with st.sidebar:
    st.header("⚙️ الإعدادات")
    api_key = st.text_input("Gemini API Key", type="password")
    # تم تثبيت الموديل على 1.5 flash لضمان الاستقرار
    model_choice = st.selectbox("الموديل", ["gemini-1.5-flash", "gemini-1.5-pro"])
    target_mins = st.slider("مدة الفيديو (دقائق)", 10, 60, 30)
    
    st.divider()
    if st.button("🔄 مسح الذاكرة"):
        st.session_state.clear()
        st.rerun()

# ============================================================
# تعريف العقول (البرومبتات المختصرة والقوية)
# ============================================================
MINDS = [
    {"id": 1, "name": "المُنقب", "desc": "طلع الحقائق الصادمة والزتونة العلمية للموضوع والمواقف المصرية الواقعية."},
    {"id": 2, "name": "المحلل النفسي", "desc": "حلل وجع المشاهد واكتب اعترافات بلسانه (ليه بيخجل؟ إيه اللي بيوقفه؟)."},
    {"id": 3, "name": "صائد الزاوية", "desc": "أوجد زاوية 'غير مكررة'. اقلب مفهوم المشاهد عن الموضوع تماماً."},
    {"id": 4, "name": "ملك الهوك", "desc": "اكتب أقوى مقدمة 90 ثانية تخطف المشاهد بالعامية المصرية الصارخة."},
    {"id": 5, "name": "المهندس", "desc": "ابني هيكل الفيديو (8 فصول) وازرع قنابل فضول في نهاية كل فصل."},
    {"id": 6, "name": "الحكواتي", "desc": "حول الشرح لقصص مصرية وتشبيهات من الشارع (ميكروباص، قهوة، شاحن)."},
    {"id": 7, "name": "حارس الاستبقاء", "desc"

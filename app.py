import time
import re
import streamlit as st
from google import genai

# ============================================================
# إعداد الصفحة
# ============================================================

st.set_page_config(
    page_title="غرفة كتابة يوتيوب - 10 عقول",
    page_icon="🧠",
    layout="wide",
)

st.title("🧠 غرفة كتابة سكريبت يوتيوب - نظام 10 عقول")
st.write(
    "نظام كتابة متعدد المراحل: بحث وفهم للمشاهد ← زاوية جديدة ← هوك ← رحلة سردية "
    "← قصص وأمثلة ← هندسة الاستبقاء ← مسودة كاملة ← Humanization ← نقد قاسٍ "
    "← إعادة كتابة نهائية جاهزة للتسجيل."
)

# ============================================================
# الإعدادات
# ============================================================

with st.sidebar:
    st.header("⚙️ الإعدادات")

    api_key = st.text_input(
        "Google Gemini API Key",
        type="password",
    )

    model_name = st.text_input(
        "اسم الموديل",
        value="gemini-3.6-flash",
    )

    fallback_model = st.text_input(
        "Fallback Model",
        value="gemini-3.6-flash",
    )

    target_minutes = st.slider(
        "مدة الفيديو المستهدفة بالدقائق",
        min_value=10,
        max_value=60,
        value=30,
        step=5,
    )

    audience = st.text_area(
        "الجمهور المستهدف",
        value=(
            "شباب وبنات من 18 إلى 35 سنة، عايزين يفهموا نفسهم ويحسنوا حياتهم، "
            "لكن بيكرهوا الكلام الوعظي والتنمية البشرية المكررة، وبيخرجوا بسرعة "
            "لو حسوا إن الفيديو بيحاضر عليهم أو بيقول كلام عام."
        ),
        height=130,
    )

    sources = st.text_area(
        "مصادر أو كتب أو ملاحظات اختيارية",
        placeholder="اكتب هنا كتاب، بحث، ملاحظات، أفكار، أو أي مادة عايز السكريبت يعتمد عليها.",
        height=140,
    )

    st.divider()

    show_all_outputs = st.checkbox(
        "عرض مخرجات كل العقول",
        value=True,
    )


# ============================================================
# القواعد العامة
# ============================================================

GLOBAL_RULES = """
أنت عضو داخل غرفة كتابة حقيقية لفيديو يوتيوب طويل.

الهدف النهائي ليس إنتاج مقال، ولا تلخيص كتاب، ولا محاضرة في علم النفس.
الهدف هو بناء تجربة مشاهدة تجعل شخصًا حقيقيًا يشعر في أول لحظات أن:
"الفيديو ده بيتكلم عن حاجة أنا عايشها فعلاً"
ثم يكمل لأنه يريد أن يعرف كيف تتكشف الفكرة.

اكتب بالعربية العامية المصرية الطبيعية المناسبة للكلام أمام الكاميرا.

القواعد غير القابلة للكسر:

1. لا تبدأ بتحية.
2. لا تبدأ بتعريف أكاديمي.
3. ممنوع: "في الفيديو ده هنتكلم عن".
4. ممنوع: "متعرفش إن..." و"هل تعلم أن..." و"انت نايم على..." وأي كليشيه مشابه.
5. ممنوع استخدام "استنى للنهاية" كحيلة للاستبقاء.
6. لا تخترع دراسة أو رقمًا أو تجربة أو اسم عالم.
7. لو معلومة غير مؤكدة، علّم عليها بوضوح.
8. لا تستخدم مصطلحًا علميًا إلا لو سيساعد فعلًا في تفسير تجربة إنسانية.
9. ابدأ بما يعيشه الإنسان، ثم فسّر لماذا قد يحدث.
10. لا تكرر نفس الفكرة بصياغتين مختلفتين لمجرد إطالة النص.
11. لا تجعل النص كله جملًا قصيرة؛ التنوع الطبيعي في طول الجمل أهم.
12. لا تجعل كل 15 ثانية سؤالًا أو مفاجأة مصطنعة.
13. المقصود بالاستبقاء: تغيير حقيقي في الحالة، مثل:
    موقف، اعتراف، اعتراض، اكتشاف، قصة، نتيجة، مثال، تغيير منظور، إجابة،
    مفارقة، أو فتح سؤال حقيقي له إجابة لاحقة.
14. كل جزء من الفيديو يجب أن يدفع المشاهد للأمام.
15. لا تعامل المشاهد كأنه غبي، ولا تحاضر عليه.
16. لا تستخدم نبرة "أنا هصلح حياتك".
17. لا تقل للمشاهد إنه لازم يعمل شيئًا إلا عندما يكون التطبيق نتيجة طبيعية لما اكتشفه.
18. لا تستخدم لغة ذكاء اصطناعي أو لغة مقالات مثل:
    "في عالم يتسم بـ"، "من الجدير بالذكر"، "دعنا نتعمق"،
    "هذا يقودنا إلى"، "الأمر الذي يجعلنا".
19. الكلام لازم يتقال بصوت إنسان، مش يتقري من مقال.
20. لو الموضوع يستحق 18 دقيقة فقط، لا تحشوه إلى 30 دقيقة.
21. أي تشبيه هو وسيلة للفهم، وليس حقيقة علمية.
22. لا تكتب سكريبتًا يشبه سلسلة نصائح منفصلة.
23. الفيديو يجب أن يكون رحلة اكتشاف واحدة لها بداية وتحول ونهاية.
"""


COMMON_INPUTS = """
================ بيانات المشروع ================

فكرة أو عنوان الفيديو:
{topic}

الجمهور المستهدف:
{audience}

المدة المستهدفة:
{target_minutes} دقيقة

مصادر أو كتب أو ملاحظات المستخدم:
{sources}

=================================================
"""


# ============================================================
# العقول العشرة
# ============================================================

MINDS = [

    # --------------------------------------------------------
    # العقل 1
    # --------------------------------------------------------
    {
        "id": 1,
        "name": "العقل 1 — الباحث ومفكك المشكلة",
        "role": "Researcher + Problem Deconstructor",
        "description": 'أنت العقل الأول: الباحث ومفكك المشكلة. مهمتك ليست جمع معلومات كثيرة، بل اكتشاف السؤال الحقيقي المختبئ خلف عنوان المستخدم، ثم بناء مادة موثوقة تصلح لرحلة يوتيوب طويلة بالعامية المصرية.\n\nحلل الموضوع في خمس طبقات: المشكلة اليومية التي يشعر بها المشاهد، السؤال النفسي أو الفكري الأعمق، كيف كان الناس يتعاملون مع الموضوع قبل البيئة الرقمية أو قبل انتشار الفكرة، ماذا تغير في حياتنا اليوم، وما المفارقة التي تجعل الإجابة غير بديهية. ابحث عن كتب ومؤلفين ودراسات وحالات واقعية وتواريخ فقط عندما تكون قابلة للتحقق. لا تخترع نسباً أو أسماء أو اقتباسات. إذا لم تملك تحققاً كافياً، اكتب [يحتاج تحقق] ولا تحول التخمين إلى حقيقة.\n\nأخرج ملف بحث منظم لا يشبه مقالة جاهزة: (1) السؤال المركزي، (2) 3 أسئلة فرعية، (3) خط زمني أو سياق تاريخي إن كان مناسباً، (4) 5 إلى 8 أفكار موثقة، (5) قصة أو حالة إنسانية، (6) 3 مفارقات، (7) ما الذي لا ينبغي قوله لأنه مبالغ فيه، (8) قائمة المصادر ودرجة الثقة في كل ادعاء. لكل معلومة اشرح كيف تتحول إلى مشهد أو مثال مصري مفهوم.\n\nاكتب بصوت باحث فضولي لا بصوت مدرس. لا تقدم نصائح في هذه المرحلة، ولا تبدأ بالحل. المطلوب أن تمنح باقي العقول مادة تجعل السكريبت رحلة اكتشاف لا تجميع معلومات.',
    },

    # --------------------------------------------------------
    # العقل 2
    # --------------------------------------------------------
    {
        "id": 2,
        "name": "العقل 2 — محلل نفسية المشاهد",
        "role": "Viewer Psychologist",
        "description": 'أنت العقل الثاني: محلل نفسية المشاهد. أمامك الموضوع وملف البحث. افترض أن المشاهد شخص عادي لديه يوم مزدحم وتجربة شخصية مع المشكلة، وليس طالب علم. اكتشف ماذا يقول لنفسه في السر، ماذا يخجل من الاعتراف به، وما الاعتراض الذي سيقوله أثناء المشاهدة: «أنا عارف الكلام ده»، «جربت ومفيش فايدة»، «ده تنظير»، أو «المشكلة في إرادتي».\n\nاكتب خريطة شعورية من بداية الفيديو إلى نهايته: فضول، اعتراف، قلق، فهم أول، شك، مفاجأة، راحة، مسؤولية، ثم رغبة في تجربة خطوة صغيرة. لكل مرحلة اقترح سؤالاً أو موقفاً أو مثالاً، وليس مجرد عبارة تحفيزية. حدد ما الذي يجب ألا نكشفه في البداية، وما المعلومة التي تستحق أن نؤجلها حتى تتكون رغبة حقيقية في معرفة الإجابة.\n\nحافظ على أسلوب المرجعيات: المقدم في رحلة مع المشاهد، يعترف أحياناً بأنه عاش السؤال أو لم يكن يحب الشيء أو كان يفهمه غلط، لكن لا تخترع تجربة للمقدم. علّم أي اعتراف يحتاج معلومات من صاحب القناة بوسم [تجربة المقدم]. أخرج أيضاً اعتراضات المشاهد وإجاباتها داخل الرحلة، وقواعد مخاطبة مصرية طبيعية من غير تهويل أو وعظ.',
    },

    # --------------------------------------------------------
    # العقل 3
    # --------------------------------------------------------
    {
        "id": 3,
        "name": "العقل 3 — صائد الزاوية والاختراق",
        "role": "Big Idea + Unique Angle Strategist",
        "description": 'أنت العقل الثالث: صائد الزاوية والاختراق. مهمتك اختيار الفكرة الواحدة التي سيبنى عليها الفيديو كله. لا تقترح عشرة عناوين منفصلة فقط؛ قارن بين الزوايا ثم اختر زاوية مركزية على شكل مفارقة أو سؤال كبير.\n\nابنِ 5 زوايا، ولكل زاوية اكتب: السؤال، الوعد، المفارقة، القصة التي تفتحها، خطرها، وما الذي يجعلها مختلفة عن نصائح الإنترنت. بعدها اختر زاوية واحدة وفق هذه القاعدة: يجب أن تربط تجربة شخصية عادية بسؤال أكبر، وتسمح بذكر كتاب أو دراسة أو قصة، وتنتهي بتغيير في طريقة نظر المشاهد لا بقائمة نصائح.\n\nالزاوية المفضلة ليست «كيف تفعل كذا في 5 خطوات»، بل شيء من نوع: هل المشكلة التي نعتبرها عيباً لها وظيفة؟ كيف انتقلنا من خوف قديم إلى مشكلة حديثة؟ هل كثرة الشيء الذي نريده أفسدت علاقتنا به؟ اكتب الفكرة المحورية في جملة واحدة، ثم 3 جمل تشرح لماذا تستحق 20 أو 30 دقيقة. ممنوع استخدام عبارات صادمة بلا معنى أو وعد بنتيجة مضمونة.',
    },

    # --------------------------------------------------------
    # العقل 4
    # --------------------------------------------------------
    {
        "id": 4,
        "name": "العقل 4 — مهندس الهوك والبداية",
        "role": "Hook + Opening Specialist",
        "description": 'أنت العقل الرابع: مهندس الهوك والبداية. اكتب 5 بدايات مختلفة، لكن كل بداية يجب أن تفتح رحلة حقيقية لا أن تنتهي في أول 15 ثانية. استخدم واحداً من هذه الأشكال: مقارنة بين فكرتين أو كتابين، موقف يومي مألوف، سؤال شخصي محرج، مفارقة تاريخية، أو اعتراف صادق من المقدم إذا كان متاحاً.\n\nالافتتاحية المطلوبة من 90 إلى 180 ثانية تقريباً. تبدأ بصورة أو سؤال، ثم توسع المعنى، ثم تقول للمشاهد لماذا الموضوع يخص حياته، ثم تضع وعداً محدداً لما سنكتشفه، ثم تمهد للدخول في الفصل الأول. لا تقل «في الفيديو ده هقولك 10 نصائح»، ولا تستخدم «خليك للآخر» إلا إذا كان هناك سبب قصصي واضح. لا تكشف الإجابة النهائية في أول دقيقة.\n\nاستخدم جمل منطوقة مصرية، وقفات، وتغييراً بسيطاً في الإيقاع. الهوك ليس صراخاً؛ هو عقد فضول. في نهاية كل اقتراح اكتب السؤال الذي سيظل معلقاً في ذهن المشاهد. اختر أفضل افتتاحية وحدد المشهد الافتتاحي، الجملة الأولى، الوعد، وما يجب حذفه لأنه كليشيه.',
    },

    # --------------------------------------------------------
    # العقل 5
    # --------------------------------------------------------
    {
        "id": 5,
        "name": "العقل 5 — مهندس الرحلة السردية",
        "role": "Narrative Architect",
        "description": 'أنت العقل الخامس: مهندس الرحلة السردية. حوّل البحث والزاوية إلى خريطة فصول، لا إلى عناوين دروس. صمم 6 إلى 9 فصول مترابطة، وكل فصل يجب أن يجيب عن سؤال ويفتح سؤالاً تالياً.\n\nاستخدم هذا القوس عند ملاءمته للموضوع: الفصل الأول يضع التجربة اليومية والسؤال الكبير؛ الفصل الثاني «كيف كنا؟» أو ما السياق القديم؛ الفصل الثالث يشرح المفهوم أو الكتاب بلغة بسيطة؛ الفصل الرابع يقدم قصة أو حالة واقعية؛ الفصل الخامس يقلب الفكرة بمفارقة؛ الفصل السادس يربطها بحياتنا الرقمية والاجتماعية؛ الفصل السابع يوضح الثمن أو الخطر؛ الفصل الثامن يقدم حلولاً تدريجية؛ الفصل الأخير يعود إلى المشهد الأول بمعنى جديد.\n\nلكل فصل اكتب: عنواناً بسيطاً، السؤال، وظيفة الفصل، المشهد أو المثال، المصدر المناسب، نقطة الكشف، الجملة الانتقالية، وما الذي يجب عدم قوله مبكراً. لا تسمح بفصل يكرر السابق. اترك مساحة لرحلة المقدم الشخصية، لكن علّمها [تجربة المقدم] إذا لم تكن متاحة. ضع تقديراً تقريبياً للزمن، واجعل الحل نتيجة طبيعية للفهم لا دخيلة على القصة.',
    },

    # --------------------------------------------------------
    # العقل 6
    # --------------------------------------------------------
    {
        "id": 6,
        "name": "العقل 6 — راوي القصص ومترجم الأفكار للحياة",
        "role": "Storyteller + Human Examples",
        "description": 'أنت العقل السادس: راوي القصص ومترجم الأفكار للحياة. مهمتك تحويل المفاهيم والكتب والدراسات إلى مشاهد يقدر المشاهد أن يراها في يومه. لا تحوّل القصة إلى ملخص كتاب.\n\nاقترح 4 إلى 6 قصص أو أمثلة: شخص حقيقي موثق، تجربة من الكتاب، موقف مصري يومي، تشبيه ممتد، وتجربة شخصية محتملة يكتبها صاحب القناة فقط إذا كانت صحيحة. لكل قصة اكتب نقطة البداية، الصراع، التفصيلة الإنسانية، المفاجأة، والفكرة التي ستخرج منها. لا تستخدم قصة لمجرد الزينة.\n\nترجم كل مفهوم علمي أو فكري إلى ثلاثة مستويات: جملة عادية، تشبيه بصري، ثم تطبيق على موقف. بعد ذلك اكتب انتقالاً طبيعياً من القصة إلى الفكرة ومن الفكرة إلى السؤال التالي. استخدم لغة مصرية شفوية فيها تنوع، ولا تكرر «تخيل معايا» أو «الحقيقة إن» آلياً. ممنوع اختراع حوار أو تفاصيل عن شخص حقيقي؛ أي فجوة تُعلّم [يحتاج تحقق].',
    },

    # --------------------------------------------------------
    # العقل 7
    # --------------------------------------------------------
    {
        "id": 7,
        "name": "العقل 7 — مهندس الاستبقاء والإيقاع",
        "role": "Retention + Rhythm Architect",
        "description": 'أنت العقل السابع: مهندس الاستبقاء والإيقاع. لا تتعامل مع الاحتفاظ بالمشاهد على أنه صدمة كل 15 ثانية. في هذا النوع من السكريبتات، الاستبقاء يأتي من سؤال مؤجل، كشف تدريجي، قصة، انتقال، وعودة لفكرة محورية.\n\nابنِ خريطة إيقاع للفيديو: نقطة جذب كل 45 إلى 90 ثانية تقريباً، وكشف أو قلب معنى كل 2 إلى 4 دقائق. لكل نقطة حدد هل هي سؤال، مثال، مصدر، مفارقة، اعتراف، أو تطبيق. راجع أن كل 40 إلى 60 ثانية شرحاً مجرداً يتبعها مشهد أو مثال أو سؤال. اقترح وقفات وتغييرات نبرة فقط عندما تخدم المعنى.\n\nاكتب انتقالات كاملة بين الفصول، لا عناوين عامة. احذر من الإفراط في العناوين والـ B-roll والعبارات الدعائية. اجعل الفيديو يتنفس: مقطع هادئ، ثم قصة، ثم شرح، ثم سؤال. أخرج أيضاً قائمة بالأماكن التي قد يمل عندها المشاهد، مع إصلاح محدد لكل مكان، وقائمة بالمعلومات التي يجب تأجيلها كي لا يُقتل الفضول.',
    },

    # --------------------------------------------------------
    # العقل 8
    # --------------------------------------------------------
    {
        "id": 8,
        "name": "العقل 8 — الكاتب الرئيسي للمسودة",
        "role": "Long-form YouTube Script Writer",
        "description": 'أنت العقل الثامن: الكاتب الرئيسي للمسودة. أمامك كل الملفات السابقة. اكتب مسودة فيديو طويلة بالعامية المصرية، لكن لا تكتبها كدليل نصائح ولا كمقال علمي. اكتبها كرحلة صانع محتوى يتساءل ويبحث ويفهم مع المشاهد.\n\nالبنية الإلزامية: افتتاحية من 90 إلى 180 ثانية تبدأ بموقف أو مقارنة أو سؤال، ثم وعد صادق وتعريف بعلاقة المقدم بالموضوع إن وُجدت؛ فصول واضحة من غير تقطيع مصطنع؛ سياق «كيف كنا وكيف صرنا» عندما يناسب؛ ذكر الكتب والمؤلفين والدراسات والتواريخ مع صياغة دقيقة؛ قصة أو تجربة؛ مفارقة تقلب الفهم؛ تطبيقات قليلة متدرجة؛ خاتمة تعود إلى الصورة الأولى.\n\nاكتب للمسموع: جمل قصيرة ومتوسطة مختلطة، فواصل، وقفات، وأسئلة في مواضعها. لا تجعل كل فقرة تنتهي بـ«لكن المفاجأة»، ولا تحوّل كل 15 ثانية إلى هوك. استخدم إعادة جذب معنوية كل دقيقة تقريباً، وكشفاً جديداً كل عدة دقائق. اجعل المقدم إنساناً لا مذيعاً مثالياً: يمكنه أن يقول «أنا كمان كنت فاهمها غلط» فقط إذا كانت موجودة في مدخلات المستخدم، وإلا استخدم [تجربة المقدم].\n\nلا تضف معلومة غير موجودة في البحث. لا تبالغ في الجزم الطبي أو النفسي. لا تكتب مقدمة تفسيرية خارج السكريبت، وأخرج مسودة قابلة للقراءة والتسجيل مع علامات [وقفة] و[B-ROLL] عند الحاجة فقط.',
    },

    # --------------------------------------------------------
    # العقل 9
    # --------------------------------------------------------
    {
        "id": 9,
        "name": "العقل 9 — المحرر البشري القاسي",
        "role": "Humanization Editor + Brutal Script Doctor",
        "description": 'أنت العقل التاسع: المحرر البشري القاسي وطبيب السكريبت. قارن المسودة مع البصمة المرجعية، وليس مع قواعد كتابة عامة فقط. اسأل: هل بدأنا من سؤال إنساني؟ هل ظهر المقدم كشخص في رحلة؟ هل توجد «كيف كنا/كيف صرنا» أو خلفية تبرر الموضوع؟ هل المصادر والكتب جزء من الحكاية؟ هل كل فصل يفتح الذي بعده؟ هل الحل جاء بعد الفهم؟ هل رجعنا لصورة البداية؟\n\nراجع النص على 10 محاور من 10 درجات: الصدق الإنساني، قوة البداية، وضوح السؤال المركزي، حضور السياق، استخدام المصادر، جودة القصص، التدرج الفكري، الإيقاع المنطوق، طبيعية العامية المصرية، وقوة الخاتمة. اذكر أمثلة حرفية قصيرة من المسودة، ثم اقترح بديلاً لكل مشكلة.\n\nعلّم كل جملة تبدو كأنها مولدة آلياً، وكل تشبيه مكرر، وكل ادعاء زائد، وكل انتقال فج، وكل نصيحة عامة. لا تعيد كتابة النص كله. أخرج تقرير إصلاح مرتباً حسب الأولوية للعقل العاشر، مع قائمة «لا تلمسها» للأجزاء الإنسانية الناجحة. لا تطلب هوكاً كل 15 ثانية؛ اطلب سؤالاً أو كشفاً جديداً عندما يضعف المعنى.',
    },

    # --------------------------------------------------------
    # العقل 10
    # --------------------------------------------------------
    {
        "id": 10,
        "name": "العقل 10 — الكاتب النهائي والمخرج الداخلي",
        "role": "Final Master Writer + Viewer Advocate",
        "description": 'أنت العقل العاشر: الكاتب النهائي والمخرج الداخلي. أمامك البحث، خريطة المشاهد، المسودة، ونقد المحرر. أعد كتابة سكريبت واحد متماسك بصوت مصري إنساني، لا تجمع فقرات العقول فوق بعضها.\n\nالتزم بروح المرجعيات العامة: رحلة تبدأ من موقف أو سؤال، ثم وعد صادق، ثم حضور المقدم كإنسان يبحث لا كخبير معصوم، ثم سياق أو تاريخ يوضح كيف وصلنا إلى هنا، ثم كتاب أو دراسة أو قصة تُستخدم لفهم الفكرة، ثم مفارقة تقلب المعنى، ثم ربط بالواقع المصري الحالي، ثم تطبيق تدريجي، ثم عودة للمشهد الأول بخاتمة فيها معنى. لا تنسخ أي جملة أو استعارة مميزة من المرجعيات.\n\nاجعل المقدمة بين 90 و180 ثانية، والفصول تتنفس ولا تبدو كقائمة. استخدم سؤالاً أو كشفاً أو مثالاً كل 45 إلى 90 ثانية تقريباً، وليس خدعة صاخبة كل 15 ثانية. اجعل كل فقرة قابلة للنطق، واستخدم العامية المصرية الطبيعية من غير إدخال خليجي أو فصحى ثقيلة. احذف التكرار، والوعود المضمونة، والتفاؤل المصطنع، والشرح الذي لا يغير فهم المشاهد.\n\nلا تخترع تجربة شخصية أو دراسة أو رقماً أو اقتباساً. استخدم [تجربة المقدم] و[يحتاج تحقق] عند الضرورة. بعد السكريبت أضف قسم «ملاحظات تحقق قبل النشر» فقط، ثم قائمة قصيرة بالادعاءات التي تحتاج مراجعة. لا تشرح طريقة الكتابة ولا تذكر العقول ولا تقدم اعتذاراً.',
    },
]


# ============================================================
# تمرير السياق بين العقول
# ============================================================

ROUTES = {
    1: [],
    2: [1],
    3: [1, 2],
    4: [2, 3],
    5: [1, 2, 3, 4],
    6: [1, 2, 3, 5],
    7: [3, 4, 5, 6],
    8: [1, 2, 3, 4, 5, 6, 7],
    9: [2, 3, 4, 7, 8],
    10: [1, 2, 3, 4, 5, 6, 7, 8, 9],
}


# ============================================================
# أدوات مساعدة
# ============================================================

def trim_text(text, max_chars=12000):
    """
    تقليل حجم مخرجات العقول القديمة قبل تمريرها للعقل التالي.
    نحتفظ بالبداية والنهاية حتى لا يضيع القرار أو الخلاصة.
    """
    if not text:
        return ""

    if len(text) <= max_chars:
        return text

    head_size = int(max_chars * 0.65)
    tail_size = max_chars - head_size

    return (
        text[:head_size]
        + "\n\n"
        + "[... تم اختصار جزء من المخرج للحفاظ على حجم السياق ...]"
        + "\n\n"
        + text[-tail_size:]
    )


def format_context(selected_outputs):
    if not selected_outputs:
        return "لا توجد مخرجات سابقة مرتبطة بهذه المهمة."

    parts = []

    for label, value in selected_outputs.items():
        parts.append(
            f"""
================ {label} ================

{trim_text(value)}

================ نهاية {label} ================
"""
        )

    return "\n".join(parts)


def extract_retry_delay(error_text, default_delay):
    """
    يحاول استخراج مدة الانتظار لو الـ API كتب:
    retry in 40.18s
    """
    match = re.search(
        r"retry(?:\s+in)?\s+([0-9]+(?:\.[0-9]+)?)\s*s",
        error_text,
        flags=re.IGNORECASE,
    )

    if match:
        try:
            return max(1, float(match.group(1)))
        except ValueError:
            pass

    return default_delay


def ask_gemini(
    client,
    prompt,
    requested_model,
    fallback_model,
    max_retries=6,
    base_delay=5,
    status_slot=None,
):
    """
    استدعاء Gemini مع:
    - إعادة محاولة عند 429 / 503.
    - احترام مدة Retry لو ظهرت داخل رسالة الخطأ.
    - Fallback لو الموديل غير موجود.
    """

    current_model = (
        requested_model.strip()
        if requested_model and requested_model.strip()
        else fallback_model.strip()
    )

    fallback = (
        fallback_model.strip()
        if fallback_model and fallback_model.strip()
        else current_model
    )

    last_exc = None

    for attempt in range(1, max_retries + 1):

        try:
            response = client.models.generate_content(
                model=current_model,
                contents=prompt,
            )

            text = getattr(response, "text", "") or ""

            if not text.strip():
                raise RuntimeError("تم استلام رد فارغ من الموديل.")

            return text

        except Exception as exc:

            last_exc = exc

            error_text = str(exc)
            error_lower = error_text.lower()

            not_found = (
                "404" in error_text
                or "not_found" in error_lower
                or "not found" in error_lower
                or "no longer available" in error_lower
            )

            overloaded = (
                "429" in error_text
                or "503" in error_text
                or "resource_exhausted" in error_lower
                or "unavailable" in error_lower
                or "overloaded" in error_lower
                or "high demand" in error_lower
                or "quota" in error_lower
            )

            # ------------------------------------------------
            # لو الموديل غير موجود
            # ------------------------------------------------
            if not_found and current_model != fallback:

                if status_slot is not None:
                    status_slot.warning(
                        f"⚠️ الموديل '{current_model}' غير متاح. "
                        f"جاري التجربة بالموديل البديل '{fallback}'."
                    )

                current_model = fallback
                continue

            # ------------------------------------------------
            # لو حصل Rate Limit أو ضغط
            # ------------------------------------------------
            if overloaded and attempt < max_retries:

                exponential_delay = base_delay * (2 ** (attempt - 1))

                retry_delay = extract_retry_delay(
                    error_text,
                    exponential_delay,
                )

                wait_time = max(retry_delay, exponential_delay)

                # سقف حتى لا ينتظر المستخدم وقتًا غير منطقي
                wait_time = min(wait_time, 120)

                if status_slot is not None:
                    status_slot.info(
                        f"⏳ Gemini طلب الانتظار بسبب ضغط أو حد استخدام. "
                        f"المحاولة {attempt}/{max_retries}. "
                        f"الانتظار حوالي {int(wait_time)} ثانية..."
                    )

                time.sleep(wait_time)
                continue

            raise

    raise last_exc


def build_prompt(
    mind,
    topic,
    audience_text,
    source_text,
    target_minutes,
    outputs,
):

    route_ids = ROUTES.get(mind["id"], [])

    selected_outputs = {}

    for mind_id in route_ids:
        key = f"العقل {mind_id}"

        if key in outputs and outputs[key].strip():
            selected_outputs[key] = outputs[key]

    project_context = COMMON_INPUTS.format(
        topic=topic,
        audience=audience_text,
        target_minutes=target_minutes,
        sources=source_text.strip()
        if source_text and source_text.strip()
        else "لا توجد مصادر إضافية قدمها المستخدم.",
    )

    return f"""
{GLOBAL_RULES}

{project_context}

================================================
اسم العقل:
{mind["name"]}

التخصص:
{mind["role"]}
================================================

تعليماتك الخاصة:

{mind["description"]}

================================================
مخرجات العقول السابقة المرتبطة بمهمتك
================================================

{format_context(selected_outputs)}

================================================
طريقة التعامل مع المخرجات السابقة
================================================

1. لا تعتبر أي مخرج سابق حقيقة مقدسة.
2. لو وجدت تكرارًا، لا تكرره.
3. لو وجدت تناقضًا، اختر القرار الأفضل واذكر السبب إذا كانت مهمتك تحتاج ذلك.
4. لا تحاول إظهار أنك قرأت كل شيء بتلخيصه مرة أخرى.
5. أضف قيمة جديدة واضحة.
6. لا تكتب كلامًا تنظيريًا للعقل التالي.
7. المخرج يجب أن يكون عمليًا وقابلًا للاستخدام.
8. التزم بمهمة عقلك فقط.
"""


# ============================================================
# Session State
# ============================================================

if "pipeline_outputs" not in st.session_state:
    st.session_state.pipeline_outputs = {}

if "pipeline_topic" not in st.session_state:
    st.session_state.pipeline_topic = ""

if "last_error" not in st.session_state:
    st.session_state.last_error = ""


# ============================================================
# إدخال الموضوع
# ============================================================

topic = st.text_area(
    "🎯 اكتب فكرة أو عنوان الفيديو",
    height=150,
    placeholder=(
        "مثال: ليه كل مرة ببدأ عادة جديدة وبعدين برجع لنقطة الصفر؟\n"
        "أو: الثقة بالنفس... ليه في ناس شكلهم واثقين رغم إنهم خايفين؟"
    ),
)


# ============================================================
# أزرار التحكم
# ============================================================

has_progress = len(st.session_state.pipeline_outputs) > 0

if has_progress:
    run_label = "▶️ كمّل من آخر عقل وقفنا عنده"
else:
    run_label = "🚀 شغّل نظام الـ10 عقول"

col_run, col_reset = st.columns([3, 1])

with col_run:
    run_button = st.button(
        run_label,
        type="primary",
        use_container_width=True,
    )

with col_reset:
    reset_button = st.button(
        "🔄 ابدأ من جديد",
        use_container_width=True,
    )


if reset_button:

    st.session_state.pipeline_outputs = {}
    st.session_state.pipeline_topic = ""
    st.session_state.last_error = ""

    st.rerun()


if has_progress:

    completed = len(st.session_state.pipeline_outputs)

    st.info(
        f"📌 تم حفظ {completed} من {len(MINDS)} عقول. "
        "لو حصل Rate Limit أو خطأ، تقدر تكمل بدون إعادة العقول اللي خلصت."
    )


# ============================================================
# التشغيل
# ============================================================

if run_button:

    if not api_key.strip():
        st.error("❌ اكتب Gemini API Key الأول.")
        st.stop()

    if not topic.strip():
        st.warning("⚠️ اكتب فكرة الفيديو أو العنوان الأول.")
        st.stop()

    # لو المستخدم غيّر الموضوع، امسح التقدم القديم
    if (
        st.session_state.pipeline_topic
        and st.session_state.pipeline_topic != topic.strip()
    ):

        st.warning(
            "⚠️ تم تغيير موضوع الفيديو، لذلك سيتم بدء Pipeline جديد."
        )

        st.session_state.pipeline_outputs = {}

    st.session_state.pipeline_topic = topic.strip()
    st.session_state.last_error = ""

    try:

        client = genai.Client(
            api_key=api_key.strip()
        )

        outputs = st.session_state.pipeline_outputs

        total_minds = len(MINDS)
        completed_count = len(outputs)

        progress = st.progress(
            completed_count / total_minds
            if completed_count
            else 0
        )

        status = st.empty()

        st.divider()
        st.subheader("🧠 غرفة الكتابة")

        st.caption(
            "كل عقل يستلم فقط السياق الذي يحتاجه. "
            "العقل 8 يكتب المسودة الكاملة، العقل 9 ينتقدها، "
            "والعقل 10 يعيد كتابتها كنسخة نهائية."
        )

        for index, mind in enumerate(MINDS):

            mind_key = f"العقل {mind['id']}"

            # --------------------------------------------
            # تخطي العقل المكتمل
            # --------------------------------------------
            if mind_key in outputs:

                if show_all_outputs:

                    with st.expander(
                        f"✅ {mind['name']} — محفوظ",
                        expanded=False,
                    ):
                        st.markdown(outputs[mind_key])

                progress.progress(
                    (index + 1) / total_minds
                )

                continue

            # --------------------------------------------
            # تشغيل العقل الحالي
            # --------------------------------------------
            status.info(
                f"🧠 العقل {mind['id']}/{total_minds} يعمل الآن: "
                f"{mind['name']}"
            )

            prompt = build_prompt(
                mind=mind,
                topic=topic.strip(),
                audience_text=audience,
                source_text=sources,
                target_minutes=target_minutes,
                outputs=outputs,
            )

            result = ask_gemini(
                client=client,
                prompt=prompt,
                requested_model=model_name,
                fallback_model=fallback_model,
                status_slot=status,
            )

            if not result.strip():
                raise RuntimeError(
                    f"العقل {mind['id']} أعاد نتيجة فارغة."
                )

            # --------------------------------------------
            # حفظ مباشر
            # --------------------------------------------
            outputs[mind_key] = result

            st.session_state.pipeline_outputs = outputs

            if show_all_outputs:

                with st.expander(
                    f"🧠 {mind['name']}",
                    expanded=(mind["id"] >= 8),
                ):
                    st.markdown(result)

            progress.progress(
                (index + 1) / total_minds
            )

        # ====================================================
        # السكريبت النهائي
        # ====================================================

        final_script = outputs.get(
            "العقل 10",
            ""
        )

        status.success(
            "🎉 اكتمل نظام الـ10 عقول والسكريبت النهائي جاهز."
        )

        st.divider()

        st.header("🎬 MASTER SCRIPT")

        st.text_area(
            "📜 النسخة النهائية",
            value=final_script,
            height=1000,
        )

        st.download_button(
            label="⬇️ تحميل السكريبت النهائي TXT",
            data=final_script,
            file_name="master_youtube_script.txt",
            mime="text/plain",
            use_container_width=True,
        )

        # ====================================================
        # تحميل جميع المخرجات
        # ====================================================

        all_outputs_text = []

        for mind in MINDS:

            key = f"العقل {mind['id']}"

            if key in outputs:

                all_outputs_text.append(
                    f"""
====================================================
{mind["name"]}
====================================================

{outputs[key]}
"""
                )

        st.download_button(
            label="⬇️ تحميل تقرير جميع العقول بالكامل TXT",
            data="\n\n".join(all_outputs_text),
            file_name="all_minds_report.txt",
            mime="text/plain",
            use_container_width=True,
        )

    except Exception as e:
        st.session_state.last_error = str(e)
        st.error(f"❌ حدث خطأ أثناء المعالجة: {str(e)}")

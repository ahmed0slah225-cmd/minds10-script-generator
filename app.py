import time
import re
import streamlit as st
from google import genai

# ============================================================
# إعداد الصفحة
# ============================================================

st.set_page_config(
    page_title="غرفة كتابة يوتيوب - 10 عقول",
    page_icon="🧠",
    layout="wide",
)

st.title("🧠 غرفة كتابة سكريبت يوتيوب - نظام 10 عقول")
st.write(
    "نظام كتابة متعدد المراحل: بحث وفهم للمشاهد → زاوية جديدة → هوك → رحلة سردية "
    "→ قصص وأمثلة → هندسة الاستبقاء → مسودة كاملة → Humanization → نقد قاسٍ "
    "→ إعادة كتابة نهائية جاهزة للتسجيل."
)

# ============================================================
# الإعدادات
# ============================================================

with st.sidebar:
    st.header("⚙️ الإعدادات")

    api_key = st.text_input(
        "Google Gemini API Key",
        type="password",
    )

    model_name = st.text_input(
        "اسم الموديل",
        value="gemini-3.6-flash",
    )

    fallback_model = st.text_input(
        "Fallback Model",
        value="gemini-3.6-flash",
    )

    target_minutes = st.slider(
        "مدة الفيديو المستهدفة بالدقائق",
        min_value=10,
        max_value=60,
        value=30,
        step=5,
    )

    audience = st.text_area(
        "الجمهور المستهدف",
        value=(
            "شباب وبنات من 18 إلى 35 سنة، عايزين يفهموا نفسهم ويحسنوا حياتهم، "
            "لكن بيكرهوا الكلام الوعظي والتنمية البشرية المكررة، وبيخرجوا بسرعة "
            "لو حسوا إن الفيديو بيحاضر عليهم أو بيقول كلام عام."
        ),
        height=130,
    )

    sources = st.text_area(
        "مصادر أو كتب أو ملاحظات اختيارية",
        placeholder="اكتب هنا كتاب، بحث، ملاحظات، أفكار، أو أي مادة عايز السكريبت يعتمد عليها.",
        height=140,
    )

    st.divider()

    show_all_outputs = st.checkbox(
        "عرض مخرجات كل العقول",
        value=True,
    )


# ============================================================
# القواعد العامة
# ============================================================

GLOBAL_RULES = """
أنت عضو داخل غرفة كتابة حقيقية لفيديو يوتيوب طويل.

الهدف النهائي ليس إنتاج مقال، ولا تلخيص كتاب، ولا محاضرة في علم النفس.
الهدف هو بناء تجربة مشاهدة تجعل شخصًا حقيقيًا يشعر في أول لحظات أن:
"الفيديو ده بيتكلم عن حاجة أنا عايشها فعلاً"
ثم يكمل لأنه يريد أن يعرف كيف تتكشف الفكرة.

اكتب بالعربية العامية المصرية الطبيعية المناسبة للكلام أمام الكاميرا.

القواعد غير القابلة للكسر:

1. لا تبدأ بتحية.
2. لا تبدأ بتعريف أكاديمي.
3. ممنوع: "في الفيديو ده هنتكلم عن".
4. ممنوع: "متعرفش إن..." و"هل تعلم أن..." و"انت نايم على..." وأي كليشيه مشابه.
5. ممنوع استخدام "استنى للنهاية" كحيلة للاستبقاء.
6. لا تخترع دراسة أو رقمًا أو تجربة أو اسم عالم.
7. لو معلومة غير مؤكدة، علّم عليها بوضوح.
8. لا تستخدم مصطلحًا علميًا إلا لو سيساعد فعلًا في تفسير تجربة إنسانية.
9. ابدأ بما يعيشه الإنسان، ثم فسّر لماذا قد يحدث.
10. لا تكرر نفس الفكرة بصياغتين مختلفتين لمجرد إطالة النص.
11. لا تجعل النص كله جملًا قصيرة؛ التنوع الطبيعي في طول الجمل أهم.
12. لا تجعل كل 15 ثانية سؤالًا أو مفاجأة مصطنعة.
13. المقصود بالاستبقاء: تغيير حقيقي في الحالة، مثل:
    موقف، اعتراف، اعتراض، اكتشاف، قصة، نتيجة، مثال، تغيير منظور، إجابة،
    مفارقة، أو فتح سؤال حقيقي له إجابة لاحقة.
14. كل جزء من الفيديو يجب أن يدفع المشاهد للأمام.
15. لا تعامل المشاهد كأنه غبي، ولا تحاضر عليه.
16. لا تستخدم نبرة "أنا هصلح حياتك".
17. لا تقل للمشاهد إنه لازم يعمل شيئًا إلا عندما يكون التطبيق نتيجة طبيعية لما اكتشفه.
18. لا تستخدم لغة ذكاء اصطناعي أو لغة مقالات مثل:
    "في عالم يتسم بـ"، "من الجدير بالذكر"، "دعنا نتعمق"،
    "هذا يقودنا إلى"، "الأمر الذي يجعلنا".
19. الكلام لازم يتقال بصوت إنسان، مش يتقري من مقال.
20. لو الموضوع يستحق 18 دقيقة فقط، لا تحشوه إلى 30 دقيقة.
21. أي تشبيه هو وسيلة للفهم، وليس حقيقة علمية.
22. لا تكتب سكريبتًا يشبه سلسلة نصائح منفصلة.
23. الفيديو يجب أن يكون رحلة اكتشاف واحدة لها بداية وتحول ونهاية.
"""


COMMON_INPUTS = """
================ بيانات المشروع ================

فكرة أو عنوان الفيديو:
{topic}

الجمهور المستهدف:
{audience}

المدة المستهدفة:
{target_minutes} دقيقة

مصادر أو كتب أو ملاحظات المستخدم:
{sources}

=================================================
"""


# ============================================================
# العقول العشرة
# ============================================================

MINDS = [

    # --------------------------------------------------------
    # العقل 1
    # --------------------------------------------------------
    {
        "id": 1,
        "name": "العقل 1 — الباحث ومفكك المشكلة",
        "role": "Researcher + Problem Deconstructor",
        "description": """
مهمتك ليست كتابة السكريبت.

أنت الباحث الذي يمنع غرفة الكتابة من بناء فيديو جميل فوق فهم سطحي.

حلل الموضوع كالتالي:

أولًا: ما المشكلة الظاهرة؟
ما الشيء الذي يعتقد المشاهد أنه مشكلته؟

ثانيًا: ما الطبقات المحتملة تحت المشكلة؟
فرق بين:
- ما يراه الشخص.
- ما يشعر به.
- ما يفعله.
- ما يفسره بشكل خاطئ.
- ما قد يكون السبب أو الآلية الأعمق.

ثالثًا: ما التفسيرات السطحية الشائعة؟
ولماذا تبدو مقنعة؟
ولماذا قد تكون ناقصة أو مضللة؟

رابعًا: ابحث داخل المعرفة المتاحة لديك عن أفكار أو مبادئ أو نتائج موثوقة
تساعد على تفسير الموضوع.
لا تخترع مراجع.
لا تملأ القائمة بمعلومات كثيرة.
اختر فقط ما يمكن تحويله إلى لحظات مفيدة داخل الفيديو.

لكل فكرة علمية أو معرفية اكتب:
- الفكرة.
- ماذا تفسر في حياة المشاهد؟
- قوة الثقة: عالية / متوسطة / تحتاج تحقق.
- خطر إساءة استخدامها أو تبسيطها.

أخرج:

1. تعريف المشكلة كما يعيشها الإنسان، وليس تعريفًا أكاديميًا.
2. طبقات المشكلة.
3. 10 مواقف واقعية تظهر المشكلة.
4. 10 أفكار أو حقائق مفيدة فقط.
5. 5 خرافات أو تفسيرات ناقصة.
6. 3 أسئلة بحثية يمكن أن تحمل الفيديو.
7. قائمة بالمعلومات التي تحتاج تحقق قبل النشر.

ممنوع كتابة الهوك أو المقدمة أو السكريبت.
""",
    },

    # --------------------------------------------------------
    # العقل 2
    # --------------------------------------------------------
    {
        "id": 2,
        "name": "العقل 2 — محلل نفسية المشاهد",
        "role": "Viewer Psychologist",
        "description": """
أنت لا تشرح الموضوع.
أنت تحاول أن تعرف: من هو الشخص الذي سيضغط على الفيديو أصلًا؟

ابنِ شخصية المشاهد من الداخل.

اكتشف:

- ماذا حدث له قبل أن يبحث عن هذا الفيديو؟
- ما الموقف الذي جعله يقول: "أنا محتاج أفهم ده"؟
- ماذا يقول لنفسه وهو لوحده؟
- ما الشيء الذي يخجل أن يقوله؟
- ما الشيء الذي يلوم نفسه عليه؟
- ما التفسير الذي صدقه عن نفسه؟
- ما الذي يخاف أن يكون صحيحًا؟
- ما الذي يتمنى أن يسمعه؟
- وما الحقيقة التي يحتاج أن يسمعها حتى لو لم تكن مريحة؟

ثم اعمل "خريطة لحظة الضغط على الفيديو":

بعد 5 ثواني:
ماذا يجب أن يشعر؟

بعد 15 ثانية:
ما السؤال الذي يجب أن يكون قد دخل دماغه؟

بعد 30 ثانية:
ما الوعد الفكري أو العاطفي الذي يجعله يكمل؟

بعد دقيقتين:
لماذا لا يزال موجودًا؟

اكتب 15 جملة اعتراف داخلية بصيغة إنسانية حقيقية.
ليست أسئلة أكاديمية.

مثال على المستوى المطلوب:
"أنا كل مرة ببدأ بحاجة وأنا متحمس... وبعد كام يوم بحس إني كنت بكدب على نفسي."

لا تكتب:
"لماذا لا أستطيع الاستمرار في العادات؟"

في النهاية استخرج:
- الجملة التي تجعل المشاهد يقول: "دي أنا".
- أعمق ألم.
- أعمق خوف.
- أمل واقعي.
- أكبر سوء فهم عن نفسه.

ممنوع كتابة السكريبت أو الهوك النهائي.
""",
    },

    # --------------------------------------------------------
    # العقل 3
    # --------------------------------------------------------
    {
        "id": 3,
        "name": "العقل 3 — صائد الزاوية والاختراق",
        "role": "Big Idea + Unique Angle Strategist",
        "description": """
أمامك بحث عن الموضوع ونفسية المشاهد.

مهمتك العثور على "الفكرة التي تستحق الفيديو".

لا تقبل زاوية عامة مثل:
"الثقة بالنفس مهمة"
أو
"لازم يكون عندك إرادة"
أو
"العادات تحتاج استمرارية".

ابحث عن زاوية فيها أحد العناصر التالية:
- تناقض.
- إعادة تفسير.
- شيء يفعله المشاهد ويبدو منطقيًا لكنه قد يكون جزءًا من المشكلة.
- فرق بين ما يظنه الشخص وما يحدث فعلًا.
- رحلة من اعتقاد خاطئ إلى فهم أعمق.

اعمل 5 زوايا مختلفة للفيديو.

لكل زاوية اكتب:
1. الفكرة الكبيرة في جملة واحدة.
2. الاعتقاد القديم.
3. الاكتشاف الجديد.
4. لماذا سيهتم المشاهد؟
5. ما الخطر لو كانت الزاوية مبالغًا فيها؟
6. كيف يمكن أن تتحول إلى رحلة مدتها طويلة؟

بعد ذلك:
- قيّم الزوايا.
- اقتل الزوايا المكررة أو الضعيفة.
- اختر زاوية واحدة فقط.

ثم اكتب:
"التحول المركزي للفيديو"

بالصيغة:
في البداية، المشاهد يعتقد أن ______.
وفي النهاية، يفهم أن ______.

ثم حدد:
- السؤال المركزي.
- الصراع المركزي.
- الوعد الحقيقي.
- الشيء الذي لا يجب كشفه في أول دقيقة لأنه جزء من رحلة الاكتشاف.

لا تكتب الهوك النهائي ولا السكريبت.
""",
    },

    # --------------------------------------------------------
    # العقل 4
    # --------------------------------------------------------
    {
        "id": 4,
        "name": "العقل 4 — مهندس الهوك والبداية",
        "role": "Hook + Opening Specialist",
        "description": """
مهمتك أصعب من كتابة جملة جذابة.

أنت مسؤول عن أول دقيقة من الفيديو.

الهوك الناجح هنا يجب أن يجعل المشاهد يشعر بشيء واحد على الأقل:
- "هو عرف ده عني إزاي؟"
- "استنى... أنا عمري ما بصيت لها كده."
- "طيب لو ده صح، يبقى أنا فاهم نفسي غلط."
- "إيه اللي حصل بعد كده؟"

قبل الكتابة، استخرج:
1. أقوى موقف حقيقي.
2. أقوى تناقض.
3. الاعتقاد الذي ستقلبه.
4. السؤال المخفي داخل الموضوع.

اكتب 10 افتتاحيات مختلفة تمامًا.
لا تغيّر كلمات في نفس الفكرة.

استخدم تنويعًا بين:
- الدخول في منتصف موقف.
- اعتراف صريح.
- مشهد يومي.
- جملة تقلب الاعتقاد.
- حوار داخلي.
- بداية قصة في لحظة توتر.
- مواجهة هادئة.
- تفصيلة صغيرة تكشف مشكلة كبيرة.

كل افتتاحية من 15 إلى 35 ثانية.

بعدها قيّم كل واحدة حسب:
- الفضول الحقيقي.
- مدى خصوصيتها.
- مدى صدقها.
- هل تصلح لفيديو طويل أم مجرد Clickbait؟
- هل تكشف الإجابة بدري؟

اقتل الضعيف.

اختر أفضل 3.

ثم ابنِ أفضل افتتاحية إلى أول 90 ثانية كاملة.

البداية يجب أن تمر طبيعيًا من:
Hook → موقف → سؤال → مفارقة أو تناقض → فتح رحلة الفيديو.

ممنوع:
"في الفيديو ده..."
"النهاردة هنتكلم..."
"استنى للنهاية..."
"هقولك سر..."
"المفاجأة..."

اكتب المادة بصيغة منطوقة بالمصري.
""",
    },

    # --------------------------------------------------------
    # العقل 5
    # --------------------------------------------------------
    {
        "id": 5,
        "name": "العقل 5 — مهندس الرحلة السردية",
        "role": "Narrative Architect",
        "description": """
لا تكتب السكريبت.

ابنِ رحلة واحدة تجعل الفيديو يبدو كأنه قصة اكتشاف،
وليس 7 فصول من نصائح.

خذ الزاوية المختارة والبداية المختارة.

حدد:

A. نقطة البداية:
أين يقف المشاهد نفسيًا وفكريًا؟

B. أول كسر:
ما أول شيء يجعله يشك في تفسيره القديم؟

C. الاكتشاف الأول:
ما الحقيقة التي تبدو كإجابة؟

D. التعقيد:
ما الشيء الذي يجعل الإجابة الأولى غير كافية؟

E. التحول:
أين تتغير طريقة فهمه للمشكلة فعلًا؟

F. التطبيق:
كيف يرى حياته بشكل مختلف؟

G. العودة:
كيف نرجع إلى الموقف الذي بدأنا به ونفهمه بشكل جديد؟

ابنِ تقسيمًا زمنيًا مناسبًا لمدة الفيديو.

لكل جزء اكتب:
- الزمن التقريبي.
- الهدف الدرامي.
- ما الذي يعرفه المشاهد الآن؟
- ما الجديد الذي سيكتشفه؟
- نوع المادة: موقف / قصة / شرح / مثال / اعتراض / تجربة ذهنية / تطبيق.
- السؤال أو الفجوة التي تقوده لما بعده.

أضف:
1. Open Loops Map.
2. Payoffs Map.
3. منحنى الطاقة.
4. أماكن يجب أن يهدأ فيها الفيديو حتى لا يصبح صراخًا مستمرًا.
5. كل 2-4 دقائق: ما التغيير الكبير الذي سيحدث؟

مهم جدًا:
لا تجعل كل نهاية فصل تقول:
"لكن في حاجة أهم..."
ابنِ الانتقال من منطق الفكرة نفسها.
""",
    },

    # --------------------------------------------------------
    # العقل 6
    # --------------------------------------------------------
    {
        "id": 6,
        "name": "العقل 6 — راوي القصص ومترجم الأفكار للحياة",
        "role": "Storyteller + Human Examples",
        "description": """
أنت مسؤول عن إعطاء الرحلة لحمًا ودمًا.

خذ الهيكل وحدد لكل جزء:
- مشهدًا.
- قصة قصيرة.
- مثالًا.
- حوارًا داخليًا.
- أو تجربة ذهنية.

لا تكتب قصصًا كليشيهية عن:
"شاب كان فاشل ثم نجح"
إلا لو كانت ضرورية ومختلفة فعلًا.

الأولوية للمواقف الصغيرة التي تحدث فعلًا:
- المنبه.
- المواصلات.
- شاشة الموبايل.
- فتح اللابتوب ثم قفله.
- رسالة لم ترد عليها.
- وعد قطعته لنفسك.
- جلسة مع أصحابك.
- لحظة قبل النوم.
- يوم ضاع بدون ما تحس.

لكل جزء مهم من الرحلة قدم:
1. المشهد.
2. التفاصيل الحسية الضرورية فقط.
3. ما يقوله الشخص لنفسه.
4. ما الذي يكشفه المشهد؟
5. كيف ينتقل للشرح بدون أن يقول:
"وده معناه إن..."

إذا احتجت تبسيط فكرة علمية:
ابدأ بالتجربة الإنسانية.
ثم اشرح الفكرة.
ثم استخدم مثالًا أو تشبيهًا إذا كان يساعد.

اختر تشبيهات أصلية وغير مستهلكة.

أخرج "بنك المشاهد والقصص والأمثلة"
مرتبًا حسب أجزاء الرحلة.
لا تكتب السكريبت النهائي كاملًا.
""",
    },

    # --------------------------------------------------------
    # العقل 7
    # --------------------------------------------------------
    {
        "id": 7,
        "name": "العقل 7 — مهندس الاستبقاء والإيقاع",
        "role": "Retention + Rhythm Architect",
        "description": """
مهمتك حماية الفيديو من الملل.

أنت لا تريد إضافة سؤال كل 15 ثانية.
أنت تريد التأكد أن المشاهد يتحرك باستمرار.

خذ:
- البداية.
- الرحلة.
- بنك المشاهد.
- الأفكار.

وابنِ Retention Blueprint.

قسّم الفيديو إلى وحدات تقريبية من 15 إلى 30 ثانية،
لكن لا تحول الفيديو إلى جدول ميكانيكي.

لكل وحدة أو كتلة صغيرة حدد:
- حالة المشاهد الآن.
- ماذا يعرف؟
- ماذا يشعر؟
- ماذا يريد أن يعرف؟
- ما الذي تغير؟
- ما الوظيفة: موقف / كشف / اعتراض / مثال / شرح / قصة / نتيجة / تطبيق؟
- ما الجسر الطبيعي لما بعده؟

نفّذ الاختبارات التالية:

اختبار الخسارة:
لو قفل الفيديو هنا، هل هيحس إنه خسر حاجة؟
لو لا، اقترح تعديلًا.

اختبار الشرح:
لا تسمح بأكثر من 40 ثانية من شرح مجرد
بدون مثال أو صورة ذهنية أو تغيير في شكل التقديم.

اختبار التكرار:
أي فكرة اتقالت مرتين بدون إضافة جديدة تُحذف أو تندمج.

اختبار الـCallback:
حدد الأشياء التي يمكن الرجوع لها لاحقًا.

اختبار التصعيد:
كل 2-4 دقائق يجب أن يحدث واحد من:
- كشف.
- إعادة تفسير.
- إجابة لسؤال قديم.
- سؤال أكبر.
- تغيير في الرهان.
- انتقال عاطفي.

في النهاية اكتب:
"أخطر 10 أماكن قد يمل فيها المشاهد"
وما الحل لكل واحدة.

لا تكتب السكريبت النهائي.
""",
    },

    # --------------------------------------------------------
    # العقل 8
    # --------------------------------------------------------
    {
        "id": 8,
        "name": "العقل 8 — الكاتب الرئيسي للمسودة",
        "role": "Long-form YouTube Script Writer",
        "description": """
أنت الآن الكاتب الرئيسي للمسودة الأولى الكاملة.

كل ما وصلك من العقول السابقة هو مواد خام.
لا تلصقها وراء بعضها.
اختر فقط ما يخدم الرحلة.

اكتب سكريبت فيديو كامل بالمصرية الطبيعية،
جاهز تقريبًا للتسجيل.

المطلوب:

1. بداية قوية فعلًا، وليس عنوانًا بصوت عالٍ.
2. رحلة واحدة متصلة.
3. المشاهد يجب أن يشعر أن كل جزء نتيجة طبيعية لما قبله.
4. لا تستخدم عناوين مدرسية داخل الكلام.
5. استخدم القصص والمواقف كجزء من الشرح، لا كاستراحة منفصلة.
6. لا تشرح فكرة قبل أن تجعل المشاهد يشعر بالمشكلة التي تفسرها.
7. كل فترة قصيرة غيّر حالة الانتباه بشكل طبيعي.
8. لا تستخدم الأسئلة بكثرة.
9. اسمح للحظات الهادئة والعاطفية أن تتنفس.
10. التطبيق العملي لا يأتي كقائمة نصائح في النهاية.
    يجب أن يظهر كنتيجة للفهم الجديد.

أسلوب الكتابة:
- عامية مصرية.
- طبيعي.
- ذكي بدون استعراض.
- إنساني.
- فيه اعترافات وتردد ولحظات صمت عند الحاجة.
- لا يبدو كأنه ChatGPT.

صيغة الإخراج:

[HOOK]

ثم النص.

يمكن استخدام تعليمات بسيطة جدًا بين أقواس مثل:
[سكتة قصيرة]
[لقطة موبايل]
لكن لا تحول السكريبت إلى Storyboard.

المطلوب سكريبت كامل.
لا تكتب تحليلًا قبل السكريبت.
""",
    },

    # --------------------------------------------------------
    # العقل 9
    # --------------------------------------------------------
    {
        "id": 9,
        "name": "العقل 9 — المحرر البشري القاسي",
        "role": "Humanization Editor + Brutal Script Doctor",
        "description": """
أمامك مسودة سكريبت كاملة.

لا تجامل الكاتب.

اقرأ السكريبت كأنك مشاهد دخل من الصفحة الرئيسية على يوتيوب،
ومش مدين للفيديو بأي دقيقة.

افحص:

1. أول 5 ثواني:
هل هناك سبب حقيقي للاستمرار؟

2. أول 15 ثانية:
هل المشاهد دخل في مشكلة أو سؤال؟

3. أول دقيقة:
هل الفيديو فتح رحلة أم مجرد مقدمة طويلة؟

4. الاستبقاء:
أين يبدأ الشرح يتضخم؟

5. البشرية:
أي جملة تشعر أنها مكتوبة بدل ما تكون متقالة؟

6. اللهجة:
أي فصحى مترجمة أو مصطلح غريب؟

7. التكرار:
حدد الجمل أو الأفكار التي تعيد نفسها.

8. التصنع:
احذف أي:
- حكمة جاهزة.
- صدمة مصطنعة.
- سؤال مكرر.
- جملة تحاول تبدو عميقة وهي لا تضيف شيئًا.

9. العمق:
أين توجد فكرة مهمة لكن الكاتب مر عليها بسرعة؟

10. العاطفة:
هل يوجد اعتراف أو لحظة إنسانية حقيقية؟
إن لم يوجد، حدد المكان الأنسب لإضافتها.

أخرج تقريرًا صريحًا يحتوي:

A. ما الذي يعمل فعلًا؟
B. أخطر 15 مشكلة في المسودة.
C. اقتراح إعادة كتابة محدد لكل مشكلة.
D. الكلمات والتراكيب التي يجب حذفها.
E. الجمل التي يجب الحفاظ عليها.
F. خطة إعادة كتابة من البداية للنهاية.

لا تكتب السكريبت كاملًا من جديد.
أنت محرر، مهمتك جعل العقل العاشر يملك تشخيصًا واضحًا.
""",
    },

    # --------------------------------------------------------
    # العقل 10
    # --------------------------------------------------------
    {
        "id": 10,
        "name": "العقل 10 — الكاتب النهائي والمخرج الداخلي",
        "role": "Final Master Writer + Viewer Advocate",
        "description": """
أنت آخر شخص في غرفة الكتابة.

أمامك:
- فهم المشكلة.
- نفسية المشاهد.
- الزاوية.
- أفضل بداية.
- هندسة الرحلة.
- الاستبقاء.
- المسودة الكاملة.
- تقرير نقد قاسٍ.

مهمتك ليست "تصحيح أخطاء".
مهمتك إعادة كتابة النسخة النهائية كأن كاتبًا بشريًا موهوبًا
فهم كل هذه المواد ثم كتب الفيديو من جديد.

لا تنسخ المسودة حرفيًا.
احتفظ فقط بما يستحق البقاء.

قبل الكتابة نفّذ داخليًا:

اختبار 1:
لو أنا مش مهتم بالموضوع، هل أول 15 ثانية ما زالت تشدني؟

اختبار 2:
هل أعرف في كل لحظة تقريبًا لماذا أكمل؟

اختبار 3:
هل هناك جزء يمكن حذفه بدون خسارة؟
إذا نعم، احذفه أو اجعله يؤدي وظيفة جديدة.

اختبار 4:
هل النص يبدو مثل إنسان واحد يتكلم؟
أم مثل 10 روبوتات كل واحد كتب فقرة؟

اختبار 5:
هل التطبيق النهائي نتيجة طبيعية للفهم؟
أم نصائح ملزوقة؟

اختبار 6:
هل النهاية تقفل الرحلة وتعيد معنى البداية؟

================================================
شكل السكريبت النهائي
================================================

اكتب السكريبت كاملًا فقط.

ابدأ مباشرة بالنص.

لا تكتب:
"إليك السكريبت"
أو
"بالطبع"
أو أي تحليل.

يمكنك استخدام عناوين بسيطة جدًا للكاتب فقط،
لكن لا تجعلها كثيرة.

ضع تعليمات أداء أو مونتاج قليلة بين [ ] فقط عندما تكون مفيدة.

================================================
الاستبقاء
================================================

لا تطبق قاعدة "كل 15 ثانية سؤال".

بدلًا من ذلك اجعل كل فترة قصيرة يحدث تغيير حقيقي:
- صورة جديدة.
- معلومة تغير الفهم.
- قصة.
- اعتراض.
- إجابة.
- تفصيلة.
- مثال.
- لحظة صمت.
- انتقال عاطفي.
- إعادة تفسير.

================================================
الخاتمة
================================================

آخر 1.5 إلى 2 دقيقة يجب أن:

1. تعود للمشكلة أو المشهد الذي بدأ منه الفيديو.
2. تجعل المشاهد يرى البداية بشكل مختلف.
3. تلخص التحول في جملة أو جملتين بدون إعادة كل الفيديو.
4. تعطيه خطوة أو منظورًا عمليًا نابعًا من كل ما سبق.
5. تترك "لفتة أخيرة" صغيرة تعلق في ذهنه.
6. تطرح سؤال تعليق حقيقي، وليس:
   "اكتب رأيك في الكومنتات".
7. تدمج الاشتراك أو الدعم لو كان مناسبًا بشكل طبيعي جدًا.
8. تنتهي بجملة قوية صادقة، وليست حكمة مزيفة.

بعد السكريبت أضف فقط:

==============================
ملاحظات تحقق قبل النشر
==============================

واكتب تحتها فقط الادعاءات العلمية أو الأرقام أو الدراسات
التي تحتاج مراجعة.

إذا لم توجد، اكتب:
لا توجد ادعاءات محددة تحتاج تحقق إضافي.
""",
    },
]


# ============================================================
# تمرير السياق بين العقول
# ============================================================

ROUTES = {
    1: [],
    2: [1],
    3: [1, 2],
    4: [2, 3],
    5: [1, 2, 3, 4],
    6: [1, 2, 3, 5],
    7: [3, 4, 5, 6],
    8: [1, 2, 3, 4, 5, 6, 7],
    9: [2, 3, 4, 7, 8],
    10: [1, 2, 3, 4, 5, 6, 7, 8, 9],
}


# ============================================================
# أدوات مساعدة
# ============================================================

def trim_text(text, max_chars=12000):
    """
    تقليل حجم مخرجات العقول القديمة قبل تمريرها للعقل التالي.
    نحتفظ بالبداية والنهاية حتى لا يضيع القرار أو الخلاصة.
    """
    if not text:
        return ""

    if len(text) <= max_chars:
        return text

    head_size = int(max_chars * 0.65)
    tail_size = max_chars - head_size

    return (
        text[:head_size]
        + "\n\n"
        + "[... تم اختصار جزء من المخرج للحفاظ على حجم السياق ...]"
        + "\n\n"
        + text[-tail_size:]
    )


def format_context(selected_outputs):
    if not selected_outputs:
        return "لا توجد مخرجات سابقة مرتبطة بهذه المهمة."

    parts = []

    for label, value in selected_outputs.items():
        parts.append(
            f"""
================ {label} ================

{trim_text(value)}

================ نهاية {label} ================
"""
        )

    return "\n".join(parts)


def extract_retry_delay(error_text, default_delay):
    """
    يحاول استخراج مدة الانتظار لو الـ API كتب:
    retry in 40.18s
    """
    match = re.search(
        r"retry(?:\s+in)?\s+([0-9]+(?:\.[0-9]+)?)\s*s",
        error_text,
        flags=re.IGNORECASE,
    )

    if match:
        try:
            return max(1, float(match.group(1)))
        except ValueError:
            pass

    return default_delay


def ask_gemini(
    client,
    prompt,
    requested_model,
    fallback_model,
    max_retries=6,
    base_delay=5,
    status_slot=None,
):
    """
    استدعاء Gemini مع:
    - إعادة محاولة عند 429 / 503.
    - احترام مدة Retry لو ظهرت داخل رسالة الخطأ.
    - Fallback لو الموديل غير موجود.
    """

    current_model = (
        requested_model.strip()
        if requested_model and requested_model.strip()
        else fallback_model.strip()
    )

    fallback = (
        fallback_model.strip()
        if fallback_model and fallback_model.strip()
        else current_model
    )

    last_exc = None

    for attempt in range(1, max_retries + 1):

        try:
            response = client.models.generate_content(
                model=current_model,
                contents=prompt,
            )

            text = getattr(response, "text", "") or ""

            if not text.strip():
                raise RuntimeError("تم استلام رد فارغ من الموديل.")

            return text

        except Exception as exc:

            last_exc = exc

            error_text = str(exc)
            error_lower = error_text.lower()

            not_found = (
                "404" in error_text
                or "not_found" in error_lower
                or "not found" in error_lower
                or "no longer available" in error_lower
            )

            overloaded = (
                "429" in error_text
                or "503" in error_text
                or "resource_exhausted" in error_lower
                or "unavailable" in error_lower
                or "overloaded" in error_lower
                or "high demand" in error_lower
                or "quota" in error_lower
            )

            # ------------------------------------------------
            # لو الموديل غير موجود
            # ------------------------------------------------
            if not_found and current_model != fallback:

                if status_slot is not None:
                    status_slot.warning(
                        f"⚠️ الموديل '{current_model}' غير متاح. "
                        f"جاري التجربة بالموديل البديل '{fallback}'."
                    )

                current_model = fallback
                continue

            # ------------------------------------------------
            # لو حصل Rate Limit أو ضغط
            # ------------------------------------------------
            if overloaded and attempt < max_retries:

                exponential_delay = base_delay * (2 ** (attempt - 1))

                retry_delay = extract_retry_delay(
                    error_text,
                    exponential_delay,
                )

                wait_time = max(retry_delay, exponential_delay)

                # سقف حتى لا ينتظر المستخدم وقتًا غير منطقي
                wait_time = min(wait_time, 120)

                if status_slot is not None:
                    status_slot.info(
                        f"⏳ Gemini طلب الانتظار بسبب ضغط أو حد استخدام. "
                        f"المحاولة {attempt}/{max_retries}. "
                        f"الانتظار حوالي {int(wait_time)} ثانية..."
                    )

                time.sleep(wait_time)
                continue

            raise

    raise last_exc


def build_prompt(
    mind,
    topic,
    audience_text,
    source_text,
    target_minutes,
    outputs,
):

    route_ids = ROUTES.get(mind["id"], [])

    selected_outputs = {}

    for mind_id in route_ids:
        key = f"العقل {mind_id}"

        if key in outputs and outputs[key].strip():
            selected_outputs[key] = outputs[key]

    project_context = COMMON_INPUTS.format(
        topic=topic,
        audience=audience_text,
        target_minutes=target_minutes,
        sources=source_text.strip()
        if source_text and source_text.strip()
        else "لا توجد مصادر إضافية قدمها المستخدم.",
    )

    return f"""
{GLOBAL_RULES}

{project_context}

================================================
اسم العقل:
{mind["name"]}

التخصص:
{mind["role"]}
================================================

تعليماتك الخاصة:

{mind["description"]}

================================================
مخرجات العقول السابقة المرتبطة بمهمتك
================================================

{format_context(selected_outputs)}

================================================
طريقة التعامل مع المخرجات السابقة
================================================

1. لا تعتبر أي مخرج سابق حقيقة مقدسة.
2. لو وجدت تكرارًا، لا تكرره.
3. لو وجدت تناقضًا، اختر القرار الأفضل واذكر السبب إذا كانت مهمتك تحتاج ذلك.
4. لا تحاول إظهار أنك قرأت كل شيء بتلخيصه مرة أخرى.
5. أضف قيمة جديدة واضحة.
6. لا تكتب كلامًا تنظيريًا للعقل التالي.
7. المخرج يجب أن يكون عمليًا وقابلًا للاستخدام.
8. التزم بمهمة عقلك فقط.
"""


# ============================================================
# Session State
# ============================================================

if "pipeline_outputs" not in st.session_state:
    st.session_state.pipeline_outputs = {}

if "pipeline_topic" not in st.session_state:
    st.session_state.pipeline_topic = ""

if "last_error" not in st.session_state:
    st.session_state.last_error = ""


# ============================================================
# إدخال الموضوع
# ============================================================

topic = st.text_area(
    "🎯 اكتب فكرة أو عنوان الفيديو",
    height=150,
    placeholder=(
        "مثال: ليه كل مرة ببدأ عادة جديدة وبعدين برجع لنقطة الصفر؟\n"
        "أو: الثقة بالنفس... ليه في ناس شكلهم واثقين رغم إنهم خايفين؟"
    ),
)


# ============================================================
# أزرار التحكم
# ============================================================

has_progress = len(st.session_state.pipeline_outputs) > 0

if has_progress:
    run_label = "▶️ كمّل من آخر عقل وقفنا عنده"
else:
    run_label = "🚀 شغّل نظام الـ10 عقول"

col_run, col_reset = st.columns([3, 1])

with col_run:
    run_button = st.button(
        run_label,
        type="primary",
        use_container_width=True,
    )

with col_reset:
    reset_button = st.button(
        "🔄 ابدأ من جديد",
        use_container_width=True,
    )


if reset_button:

    st.session_state.pipeline_outputs = {}
    st.session_state.pipeline_topic = ""
    st.session_state.last_error = ""

    st.rerun()


if has_progress:

    completed = len(st.session_state.pipeline_outputs)

    st.info(
        f"📌 تم حفظ {completed} من {len(MINDS)} عقول. "
        "لو حصل Rate Limit أو خطأ، تقدر تكمل بدون إعادة العقول اللي خلصت."
    )


# ============================================================
# التشغيل
# ============================================================

if run_button:

    if not api_key.strip():
        st.error("❌ اكتب Gemini API Key الأول.")
        st.stop()

    if not topic.strip():
        st.warning("⚠️ اكتب فكرة الفيديو أو العنوان الأول.")
        st.stop()

    # لو المستخدم غيّر الموضوع، امسح التقدم القديم
    if (
        st.session_state.pipeline_topic
        and st.session_state.pipeline_topic != topic.strip()
    ):

        st.warning(
            "⚠️ تم تغيير موضوع الفيديو، لذلك سيتم بدء Pipeline جديد."
        )

        st.session_state.pipeline_outputs = {}

    st.session_state.pipeline_topic = topic.strip()
    st.session_state.last_error = ""

    try:

        client = genai.Client(
            api_key=api_key.strip()
        )

        outputs = st.session_state.pipeline_outputs

        total_minds = len(MINDS)
        completed_count = len(outputs)

        progress = st.progress(
            completed_count / total_minds
            if completed_count
            else 0
        )

        status = st.empty()

        st.divider()
        st.subheader("🧠 غرفة الكتابة")

        st.caption(
            "كل عقل يستلم فقط السياق الذي يحتاجه. "
            "العقل 8 يكتب المسودة الكاملة، العقل 9 ينتقدها، "
            "والعقل 10 يعيد كتابتها كنسخة نهائية."
        )

        for index, mind in enumerate(MINDS):

            mind_key = f"العقل {mind['id']}"

            # --------------------------------------------
            # تخطي العقل المكتمل
            # --------------------------------------------
            if mind_key in outputs:

                if show_all_outputs:

                    with st.expander(
                        f"✅ {mind['name']} — محفوظ",
                        expanded=False,
                    ):
                        st.markdown(outputs[mind_key])

                progress.progress(
                    (index + 1) / total_minds
                )

                continue

            # --------------------------------------------
            # تشغيل العقل الحالي
            # --------------------------------------------
            status.info(
                f"🧠 العقل {mind['id']}/{total_minds} يعمل الآن: "
                f"{mind['name']}"
            )

            prompt = build_prompt(
                mind=mind,
                topic=topic.strip(),
                audience_text=audience,
                source_text=sources,
                target_minutes=target_minutes,
                outputs=outputs,
            )

            result = ask_gemini(
                client=client,
                prompt=prompt,
                requested_model=model_name,
                fallback_model=fallback_model,
                status_slot=status,
            )

            if not result.strip():
                raise RuntimeError(
                    f"العقل {mind['id']} أعاد نتيجة فارغة."
                )

            # --------------------------------------------
            # حفظ مباشر
            # --------------------------------------------
            outputs[mind_key] = result

            st.session_state.pipeline_outputs = outputs

            if show_all_outputs:

                with st.expander(
                    f"🧠 {mind['name']}",
                    expanded=(mind["id"] >= 8),
                ):
                    st.markdown(result)

            progress.progress(
                (index + 1) / total_minds
            )

        # ====================================================
        # السكريبت النهائي
        # ====================================================

        final_script = outputs.get(
            "العقل 10",
            ""
        )

        status.success(
            "🎉 اكتمل نظام الـ10 عقول والسكريبت النهائي جاهز."
        )

        st.divider()

        st.header("🎬 MASTER SCRIPT")

        st.text_area(
            "📜 النسخة النهائية",
            value=final_script,
            height=1000,
        )

        st.download_button(
            label="⬇️ تحميل السكريبت النهائي TXT",
            data=final_script,
            file_name="master_youtube_script.txt",
            mime="text/plain",
            use_container_width=True,
        )

        # ====================================================
        # تحميل جميع المخرجات
        # ====================================================

        all_outputs_text = []

        for mind in MINDS:

            key = f"العقل {mind['id']}"

            if key in outputs:

                all_outputs_text.append(
                    f"""
====================================================
{mind["name"]}
====================================================

{outputs[key]}
"""
                )

        all_outputs_text = "\n".join(all_outputs_text)

        st.download_button(
            label="🧠 تحميل كل مخرجات العقول",
            data=all_outputs_text,
            file_name="all_10_minds_outputs.txt",
            mime="text/plain",
            use_container_width=True,
        )

        # ====================================================
        # التقرير
        # ====================================================

        st.divider()

        st.subheader("📊 تقرير المشروع")

        col1, col2, col3, col4 = st.columns(4)

        with col1:
            st.metric(
                "🧠 عدد العقول",
                len(MINDS),
            )

        with col2:
            st.metric(
                "🎬 المدة المستهدفة",
                f"{target_minutes} دقيقة",
            )

        with col3:
            estimated_blocks = int(
                (target_minutes * 60) / 20
            )

            st.metric(
                "⚡ وحدات الإيقاع التقريبية",
                f"≈ {estimated_blocks}",
            )

        with col4:
            st.metric(
                "💾 العقول المحفوظة",
                len(outputs),
            )

    except Exception as exc:

        done_count = len(
            st.session_state.pipeline_outputs
        )

        st.session_state.last_error = str(exc)

        st.error(
            f"❌ حصل خطأ بعد اكتمال "
            f"{done_count} من {len(MINDS)} عقول."
        )

        st.warning(
            "المخرجات اللي خلصت اتحفظت. "
            "بعد ما المشكلة المؤقتة تروح، دوس "
            "«كمّل من آخر عقل وقفنا عنده» "
            "ومش هيعيد العقول اللي خلصت."
        )

        st.exception(exc)


# ============================================================
# عرض السكريبت المحفوظ حتى لو التطبيق اتعمله rerun
# ============================================================

if (
    "العقل 10" in st.session_state.pipeline_outputs
    and not run_button
):

    st.divider()

    st.header("🎬 آخر سكريبت نهائي محفوظ")

    saved_final_script = st.session_state.pipeline_outputs[
        "العقل 10"
    ]

    st.text_area(
        "📜 MASTER SCRIPT",
        value=saved_final_script,
        height=800,
    )


# ============================================================
# ملاحظات
# ============================================================

st.divider()

st.caption(
    "ملاحظة: نظام الـ10 عقول يحتاج 10 طلبات API تقريبًا في التشغيل الكامل. "
    "لو الحساب المجاني وصل لحد الاستخدام أو ظهر 429، التطبيق سيحتفظ بالعقول "
    "التي انتهت ويمكنك الضغط على زر الاستكمال لاحقًا."
)

# التشغيل:
# streamlit run app.py

# التثبيت:
# pip install streamlit google-genai

import time
import re
import streamlit as st
from google import genai

# ============================================================
# إعداد الصفحة
# ============================================================

st.set_page_config(
    page_title="غرفة كتابة يوتيوب - 10 عقول",
    page_icon="🧠",
    layout="wide",
)

st.title("🧠 غرفة كتابة سكريبت يوتيوب - نظام 10 عقول")
st.write(
    "نظام كتابة متعدد المراحل: بحث وفهم للمشاهد ← زاوية جديدة ← هوك ← رحلة سردية "
    "← قصص وأمثلة ← هندسة الاستبقاء ← مسودة كاملة ← Humanization ← نقد قاسٍ "
    "← إعادة كتابة نهائية جاهزة للتسجيل."
)

# ============================================================
# الإعدادات
# ============================================================

with st.sidebar:
    st.header("⚙️ الإعدادات")

    api_key = st.text_input(
        "Google Gemini API Key",
        type="password",
    )

    model_name = st.text_input(
        "اسم الموديل",
        value="gemini-3.6-flash",
    )

    fallback_model = st.text_input(
        "Fallback Model",
        value="gemini-3.6-flash",
    )

    target_minutes = st.slider(
        "مدة الفيديو المستهدفة بالدقائق",
        min_value=10,
        max_value=60,
        value=30,
        step=5,
    )

    audience = st.text_area(
        "الجمهور المستهدف",
        value=(
            "شباب وبنات من 18 إلى 35 سنة، عايزين يفهموا نفسهم ويحسنوا حياتهم، "
            "لكن بيكرهوا الكلام الوعظي والتنمية البشرية المكررة، وبيخرجوا بسرعة "
            "لو حسوا إن الفيديو بيحاضر عليهم أو بيقول كلام عام."
        ),
        height=130,
    )

    sources = st.text_area(
        "مصادر أو كتب أو ملاحظات اختيارية",
        placeholder="اكتب هنا كتاب، بحث، ملاحظات، أفكار، أو أي مادة عايز السكريبت يعتمد عليها.",
        height=140,
    )

    st.divider()

    show_all_outputs = st.checkbox(
        "عرض مخرجات كل العقول",
        value=True,
    )


# ============================================================
# القواعد العامة
# ============================================================

GLOBAL_RULES = """
أنت عضو داخل غرفة كتابة حقيقية لفيديو يوتيوب طويل.

الهدف النهائي ليس إنتاج مقال، ولا تلخيص كتاب، ولا محاضرة في علم النفس.
الهدف هو بناء تجربة مشاهدة تجعل شخصًا حقيقيًا يشعر في أول لحظات أن:
"الفيديو ده بيتكلم عن حاجة أنا عايشها فعلاً"
ثم يكمل لأنه يريد أن يعرف كيف تتكشف الفكرة.

اكتب بالعربية العامية المصرية الطبيعية المناسبة للكلام أمام الكاميرا.

القواعد غير القابلة للكسر:

1. لا تبدأ بتحية.
2. لا تبدأ بتعريف أكاديمي.
3. ممنوع: "في الفيديو ده هنتكلم عن".
4. ممنوع: "متعرفش إن..." و"هل تعلم أن..." و"انت نايم على..." وأي كليشيه مشابه.
5. ممنوع استخدام "استنى للنهاية" كحيلة للاستبقاء.
6. لا تخترع دراسة أو رقمًا أو تجربة أو اسم عالم.
7. لو معلومة غير مؤكدة، علّم عليها بوضوح.
8. لا تستخدم مصطلحًا علميًا إلا لو سيساعد فعلًا في تفسير تجربة إنسانية.
9. ابدأ بما يعيشه الإنسان، ثم فسّر لماذا قد يحدث.
10. لا تكرر نفس الفكرة بصياغتين مختلفتين لمجرد إطالة النص.
11. لا تجعل النص كله جملًا قصيرة؛ التنوع الطبيعي في طول الجمل أهم.
12. لا تجعل كل 15 ثانية سؤالًا أو مفاجأة مصطنعة.
13. المقصود بالاستبقاء: تغيير حقيقي في الحالة، مثل:
    موقف، اعتراف، اعتراض، اكتشاف، قصة، نتيجة، مثال، تغيير منظور، إجابة،
    مفارقة، أو فتح سؤال حقيقي له إجابة لاحقة.
14. كل جزء من الفيديو يجب أن يدفع المشاهد للأمام.
15. لا تعامل المشاهد كأنه غبي، ولا تحاضر عليه.
16. لا تستخدم نبرة "أنا هصلح حياتك".
17. لا تقل للمشاهد إنه لازم يعمل شيئًا إلا عندما يكون التطبيق نتيجة طبيعية لما اكتشفه.
18. لا تستخدم لغة ذكاء اصطناعي أو لغة مقالات مثل:
    "في عالم يتسم بـ"، "من الجدير بالذكر"، "دعنا نتعمق"،
    "هذا يقودنا إلى"، "الأمر الذي يجعلنا".
19. الكلام لازم يتقال بصوت إنسان، مش يتقري من مقال.
20. لو الموضوع يستحق 18 دقيقة فقط، لا تحشوه إلى 30 دقيقة.
21. أي تشبيه هو وسيلة للفهم، وليس حقيقة علمية.
22. لا تكتب سكريبتًا يشبه سلسلة نصائح منفصلة.
23. الفيديو يجب أن يكون رحلة اكتشاف واحدة لها بداية وتحول ونهاية.
"""


COMMON_INPUTS = """
================ بيانات المشروع ================

فكرة أو عنوان الفيديو:
{topic}

الجمهور المستهدف:
{audience}

المدة المستهدفة:
{target_minutes} دقيقة

مصادر أو كتب أو ملاحظات المستخدم:
{sources}

=================================================
"""


# ============================================================
# العقول العشرة
# ============================================================

MINDS = [

    # --------------------------------------------------------
    # العقل 1
    # --------------------------------------------------------
    {
        "id": 1,
        "name": "العقل 1 — الباحث ومفكك المشكلة",
        "role": "Researcher + Problem Deconstructor",
        "description": """
مهمتك ليست كتابة السكريبت.
أنت الباحث الذي يمنع غرفة الكتابة من بناء فيديو جميل فوق فهم سطحي.

حلل الموضوع كالتالي:

أولًا: ما المشكلة الظاهرة؟
ما الشيء الذي يعتقد المشاهد أنه مشكلته؟

ثانيًا: ما الطبقات المحتملة تحت المشكلة؟
فرق بين:
- ما يراه الشخص.
- ما يشعر به.
- ما يفعله.
- ما يفسره بشكل خاطئ.
- ما قد يكون السبب أو الآلية الأعمق.

ثالثًا: ما التفسيرات السطحية الشائعة؟
ولماذا تبدو مقنعة؟ ولماذا قد تكون ناقصة أو مضللة؟

رابعًا: ابحث داخل المعرفة المتاحة لديك أو المصادر المرفقة عن أفكار أو مبادئ أو مراجع علمية ونفسية موثوقة (مثل كتب د. أليكس كورب، جيمس كلير، أو آنا ليمبكي) صغها في أفكار مبسطة.
لا تخترع مراجع، واختر فقط ما يمكن تحويله إلى لحظات مفيدة داخل الفيديو.

لكل فكرة علمية أو معرفية اكتب:
- الفكرة.
- ماذا تفسر في حياة المشاهد؟
- قوة الثقة: عالية / متوسطة / تحتاج تحقق.
- خطر إساءة استخدامها أو تبسيطها.

أخرج:
1. تعريف المشكلة كما يعيشها الإنسان، وليس تعريفًا أكاديميًا.
2. طبقات المشكلة النفسية والسلوكية.
3. 10 مواقف واقعية تظهر فيها المشكلة في الحياة اليومية.
4. 10 أفكار أو حقائق علمية ومفاهيم مفيدة فقط.
5. 5 خرافات أو تفسيرات ناقصة شائعة.
6. 3 أسئلة بحثية يمكن أن تحمل الفيديو وتصنع صراعاً.
7. قائمة بالمعلومات التي تحتاج تحقق قبل النشر.

ممنوع كتابة الهوك أو المقدمة أو السكريبت النهائي.
""",
    },

    # --------------------------------------------------------
    # العقل 2
    # --------------------------------------------------------
    {
        "id": 2,
        "name": "العقل 2 — محلل نفسية المشاهد",
        "role": "Viewer Psychologist",
        "description": """
أنت لا تشرح الموضوع، أنت تحاول أن تعرف: من هو الشخص الذي سيضغط على الفيديو أصلًا؟
ابنِ شخصية المشاهد النفسية من الداخل وقم بتفكيك الصراعات الدفينة التي يخجل من الاعتراف بها.

اكتشف:
- ماذا حدث له قبل أن يبحث عن هذا الفيديو؟ (المواقف، المعاناة اليومية، أو التعب النفسي).
- ماذا يقول لنفسه وهو لوحده؟ وما الشيء الذي يلوم نفسه عليه؟
- ما التفسير الخاطئ الذي صدقه عن نفسه؟ وما الشيء الذي يخاف أن يكون صحيحًا؟
- ما الذي يتمنى أن يسمعه؟ وما الحقيقة الواقعية التي يحتاج أن يسمعها حتى لو لم تكن مريحة؟

ثم اعمل "خريطة لحظة الضغط على الفيديو":
- بعد 5 ثواني: ماذا يجب أن يشعر؟
- بعد 15 ثانية: ما السؤال الذي يجب أن يدخل دماغه؟
- بعد 30 ثانية: ما الوعد الفكري أو العاطفي الذي يجعله يكمل؟
- بعد دقيقتين: لماذا لا يزال موجودًا؟

اكتب 15 جملة اعتراف داخلية بصيغة إنسانية حقيقية بالعامية المصرية (مثل: "أنا مبترجعش للسلوك ده عشان بحبه... أنا برجعله عشان بهرب من الوجع اللي في واقعي").

في النهاية استخرج:
- الجملة التي تجعل المشاهد يقول: "دي أنا بالظبط".
- أعمق ألم، وأعمق خوف، وأمل واقعي، وأكبر سوء فهم عن نفسه.

ممنوع كتابة السكريبت أو الهوك النهائي.
""",
    },

    # --------------------------------------------------------
    # العقل 3
    # --------------------------------------------------------
    {
        "id": 3,
        "name": "العقل 3 — صائد الزاوية والاختراق",
        "role": "Big Idea + Unique Angle Strategist",
        "description": """
أمامك بحث عن الموضوع ونفسية المشاهد. مهمتك العثور على "الفكرة أو الزاوية التي تستحق الفيديو".

لا تقبل زاوية عامة أو تنمية بشرية مستهلكة مثل: "الثقة بالنفس مهمة" أو "لازم يكون عندك إرادة".
ابحث عن زاوية فيها تناقض، إعادة تفسير، أو شيء يفعله المشاهد ويبدو منطقيًا لكنه سبب المشكلة.

اعمل 5 زوايا مختلفة للفيديو.
لكل زاوية اكتب:
1. الفكرة الكبيرة في جملة واحدة.
2. الاعتقاد القديم المكرر.
3. الاكتشاف الجديد الصادم.
4. لماذا سيهتم المشاهد؟
5. ما الخطر لو كانت الزاوية مبالغًا فيها؟
6. كيف يمكن أن تتحول إلى رحلة استكشاف طويلة؟

بعد ذلك:
- قيّم الزوايا، واقتل الزوايا المكررة، واختر زاوية واحدة حادة وقوية.

ثم اكتب "التحول المركزي للفيديو" بالصيغة:
في البداية، المشاهد يعتقد أن ______. وفي النهاية، يفهم أن ______.

ثم حدد: السؤال المركزي، الصراع المركزي، الوعد الحقيقي، والسر الذي لا يجب كشفه في أول دقيقة.

لا تكتب الهوك النهائي ولا السكريبت.
""",
    },

    # --------------------------------------------------------
    # العقل 4
    # --------------------------------------------------------
    {
        "id": 4,
        "name": "العقل 4 — مهندس الهوك والبداية",
        "role": "Hook + Opening Specialist",
        "description": """
أنت مسؤول عن صياغة البداية التي تخطف المشاهد في أول 15 إلى 90 ثانية وتربط مصيره بالفيديو.

الهوك الناجح يجب أن يثير في المشاهد: "هو عرف ده عني إزاي؟" أو "استنى... أنا عمري ما بصيت لها كده".

قبل الكتابة، استخرج:
1. أقوى موقف حقيقي.
2. أقوى تناقض.
3. الاعتقاد الذي ستقلبه.
4. السؤال المخفي داخل الموضوع.

اكتب 10 افتتاحيات مختلفة تمامًا بالعامية المصرية الحية.
نوّع بين:
- الدخول في منتصف موقف يومي صدمة.
- اعتراف صريح.
- صدمة مباشرة تشكك المشاهد في أسبابه القديمة.
- حوار داخلي.
- بداية قصة في لحظة توتر.

قيّم الـ 10 واقتل الضعيف والمبتذل، واختر أفضل 3.
ثم ابنِ أفضل افتتاحية حتى أول 90 ثانية كاملة بتسلسل:
Hook خاطف ← موقف واقعي ← سؤال معلق ← تناقض يقلب المفاهيم ← فتح وعد ورحلة الفيديو.

ممنوع: "في الفيديو ده..."، "النهاردة هنتكلم..."، "استنى للنهاية..."، "هقولك سر...".
""",
    },

    # --------------------------------------------------------
    # العقل 5
    # --------------------------------------------------------
    {
        "id": 5,
        "name": "العقل 5 — مهندس الرحلة السردية",
        "role": "Narrative Architect",
        "description": """
لا تكتب السكريبت. ابنِ رحلة متصلة تجعل الفيديو يبدو كأنه قصة اكتشاف وليس مجرد نصائح أو فصول مقطعة.

خذ الزاوية المختارة والبداية، وحدد محطات الرحلة الدرامية:
A. نقطة البداية (أين يقف المشاهد؟).
B. أول كسر (أول شك في تفسيره القديم).
C. الاكتشاف الأول (الحقيقة الأولية).
D. التعقيد (لماذا الإجابة الأولى غير كافية؟).
E. التحول النفسي (أين تتغير طريقة فهمه بالكامل؟).
F. التطبيق والمنظور الجديد.
G. العودة (الرجوع لموقف البداية بوعي مختلف).

قسّم الفيديو إلى فصول (Chapters) تتناسب مع المدة المطلوبة مع إعطائها أسماء جذابة ومستفزة فكرياً (مثل: "كذبات الدماغ"، "ميزان اللذة والألم").

لكل فصل اكتب:
- الزمن التقريبي والهدف الدرامي.
- ما يعرفه المشاهد وما سيكتشفه.
- نوع المادة (موقف، قصة، تشبيه، شرح، اعتراض، تطبيق).
- خريطة Open Loops و Payoffs ومنحنى الطاقة والتهدئة.
- الانتقال المنطقي لكل فصل بدون جمل مكررة مثل: "لكن في حاجة أهم...".
""",
    },

    # --------------------------------------------------------
    # العقل 6
    # --------------------------------------------------------
    {
        "id": 6,
        "name": "العقل 6 — راوي القصص ومترجم الأفكار للحياة",
        "role": "Storyteller + Human Examples",
        "description": """
أنت مسؤول عن إعطاء الرحلة السردية لحمًا ودمًا وعاطفة.

خذ الهيكل وقدم لكل جزء:
- مشهد ملموس من الحياة اليومية (المنبه، شاشة الموبايل، جلسة مع أصحاب، لحظة قبل النوم، يوم ضاع بدون ما تحس).
- قصص مبسطة أو تجارب من الكتب بأسلوب حكواتي دافئ دون تحويلها لمحاضرة.
- تشبيهات حية وأصلية وغير مستهلكة من الواقع المصري لتشريح المفاهيم العصبية والنفسية المعقدة (مثل تشبيه الدماغ بميزان، أو طريق السيارات، إلخ).

لكل جزء قدم:
1. المشهد بالتفاصيل الحسية الضرورية.
2. ما يقوله الشخص لنفسه في تلك اللحظة.
3. ما يكشفه المشهد وكيف ينتقل للشرح بسلاسة دون صياغة أكاديمية.

أخرج "بنك المشاهد والقصص والأمثلة والتشبيهات" مرتبًا حسب فصول الرحلة.
لا تكتب السكريبت النهائي كاملًا.
""",
    },

    # --------------------------------------------------------
    # العقل 7
    # --------------------------------------------------------
    {
        "id": 7,
        "name": "العقل 7 — مهندس الاستبقاء والإيقاع",
        "role": "Retention + Rhythm Architect",
        "description": """
مهمتك حماية الفيديو من الملل لضمان استبقاء المشاهد طوال مدة الفيديو بدون حيل رخيصة.

خذ كل المخرجات وابنِ Retention Blueprint:
1. قسّم الفيديو لمجموعات زمنية (من 15 إلى 30 ثانية).
2. حدد لكل كتلة: حالة المشاهد، ماذا يعرف، ماذا يشعر، وما التغيير الحقيقي (موقف، كشف، اعتراض، مثال، تطبيق).
3. ضع توجيهات لكسر النمط (Pattern Breaks) مثل: "استنى بس... ركز معايا في دي"، وأسئلة تفاعلية حقيقية تجعل المشاهد يراجع نفسه.
4. نفّذ الاختبارات:
   - اختبار الخسارة: لو قفل الفيديو هنا، هل يخسر قيمة؟
   - اختبار الشرح: ممنوع أكثر من 40 ثانية شرح مجرد بدون مثال أو تشبيه.
   - اختبار التصعيد: تأكد أنه كل 2-4 دقائق يحدث كشف جديد أو إعادة تفسير.

أخرج تقرير "أخطر 10 أماكن قد يمل فيها المشاهد مع الحل العملي لكل مكان"، بالإضافة لخريطة ضبط الإيقاع.
لا تكتب السكريبت النهائي.
""",
    },

    # --------------------------------------------------------
    # العقل 8
    # --------------------------------------------------------
    {
        "id": 8,
        "name": "العقل 8 — الكاتب الرئيسي للمسودة",
        "role": "Long-form YouTube Script Writer",
        "description": """
أنت الكاتب الرئيسي للمسودة الأولى الكاملة.
كل ما وصلك من العقول السابقة هو مواد خام، لا تلصقها وراء بعضها بل اختر فقط ما يخدم الرحلة.

اكتب سكريبت فيديو كامل طويل بالمصرية الطبيعية الحية، جاهز للتسجيل أمام الكاميرا.

المطلوب:
1. بداية خاطفة طبقاً للهوك المختار.
2. رحلة واحدة متصلة بأسلوب حوار إنساني، ذكي، ودافئ.
3. دمج التشبيهات والقصص والمواقف داخل الشرح.
4. الابتعاد التام عن العناوين المدرسية واللغات المعربة أو لغة الذكاء الاصطناعي (مثل "في عالم يتسم بـ"، "دعنا نتعمق").
5. جعل الخطوات العملية والتطبيق نتيجة طبيعية للفهم الجديد في السياق.

صيغة الإخراج:
[HOOK]
ثم السكريبت الكامل مع تعليمات أداء أو حركة بسيطة بين أقواس مثل: [سكتة قصيرة]، [لقطة موبايل].

اكتب المسودة الكاملة فقط بدون تحليل قبلي.
""",
    },

    # --------------------------------------------------------
    # العقل 9
    # --------------------------------------------------------
    {
        "id": 9,
        "name": "العقل 9 — المحرر البشري القاسي",
        "role": "Humanization Editor + Brutal Script Doctor",
        "description": """
أمامك مسودة السكريبت الكاملة. لا تجامل الكاتب وافحصها كأنك مشاهد يوتيوب غير صبور.

افحص وانقد بقسوة:
1. أول 5 و15 و60 ثانية: هل تشد المشاهد وتدخله في الرحلة أم مجرد مقدمة طويلة؟
2. البشرية واللهجة: أي جملة تشعر أن كاتبها روبوت أو مقال أكاديمي متصنع؟ حوّلها للسان مصري شفهي.
3. التكرار والبطء: حظر أي كلام مكرر أو حشو مجرد لإطالة المدة.
4. حذف الحكم الجاهزة والتفكير الإيجابي المزيف.
5. مطابقة الاعتراضات: هل تم التعامل مع مشاعر الإحباط والشك لدى المشاهد؟

أخرج تقريرًا صريحًا يحتوي:
A. ما الذي يعمل ببراعة؟
B. أخطر 15 مشكلة أو مواضع ضعف في المسودة.
C. مقترحات إعادة صياغة محددة وجمل بديلة بالعامية المصرية.
D. الكلمات المفترض حذفها واللحظات المفترض تعميقها.
E. خطة التعديل الموجهة للعقل العاشر.

لا تكتب السكريبت الكامل من جديد.
""",
    },

    # --------------------------------------------------------
    # العقل 10
    # --------------------------------------------------------
    {
        "id": 10,
        "name": "العقل 10 — الكاتب النهائي والمخرج الداخلي",
        "role": "Final Master Writer + Viewer Advocate",
        "description": """
أنت الساحر الأخير في غرفة الكتابة.
أمامك كل تحليلات العقول السابقة ومسودة السكريبت ونقد المحرر القاسي.

مهمتك: إعادة كتابة النسخة النهائية الكاملة للسكريبت كأن كاتبًا بشريًا عبقريًا صاغ الفيديو بنفسه من جديد بصوت واحد وروح واحدة.

شروط النسخة النهائية:
1. ابدأ فورًا بالسكريبت النهائي بدقة عالية وبالعامية المصرية الحية الشفهية.
2. تدرج رائع في الأفكار، إيقاع متزن، لغة دافئة ومستفزة فكرياً بدون تحذير أو توجيهات معقدة.
3. الخاتمة (آخر 2 دقيقة): العودة لموقف البداية بنظرة مختلفة، تلخيص التحول في جملة عميقة، تقديم خطوة عملية واقعية، وطرح سؤال تعليقي حقيقي يمس نفسية المشاهد.
4. أضف توجيهات إخراجية أو بصرية (B-Roll) بين أقواس [ ] لدعم الأفكار.

بعد السكريبت كاملاً، أضف فقرة واحدة فقط:
==============================
ملاحظات تحقق قبل النشر
==============================
واكتب تحتها أي ادعاءات أو دراسات تحتاج مراجعة، أو اكتب: "لا توجد ادعاءات محددة تحتاج تحقق إضافي."

اكتب النص النهائي مباشرة بدون أي مقدمات مثل "إليك السكريبت".
""",
    },
]


# ============================================================
# تمرير السياق بين العقول
# ============================================================

ROUTES = {
    1: [],
    2: [1],
    3: [1, 2],
    4: [2, 3],
    5: [1, 2, 3, 4],
    6: [1, 2, 3, 5],
    7: [3, 4, 5, 6],
    8: [1, 2, 3, 4, 5, 6, 7],
    9: [2, 3, 4, 7, 8],
    10: [1, 2, 3, 4, 5, 6, 7, 8, 9],
}


# ============================================================
# أدوات مساعدة
# ============================================================

def trim_text(text, max_chars=12000):
    """
    تقليل حجم مخرجات العقول القديمة قبل تمريرها للعقل التالي.
    نحتفظ بالبداية والنهاية حتى لا يضيع القرار أو الخلاصة.
    """
    if not text:
        return ""

    if len(text) <= max_chars:
        return text

    head_size = int(max_chars * 0.65)
    tail_size = max_chars - head_size

    return (
        text[:head_size]
        + "\n\n"
        + "[... تم اختصار جزء من المخرج للحفاظ على حجم السياق ...]"
        + "\n\n"
        + text[-tail_size:]
    )


def format_context(selected_outputs):
    if not selected_outputs:
        return "لا توجد مخرجات سابقة مرتبطة بهذه المهمة."

    parts = []

    for label, value in selected_outputs.items():
        parts.append(
            f"""
================ {label} ================

{trim_text(value)}

================ نهاية {label} ================
"""
        )

    return "\n".join(parts)


def extract_retry_delay(error_text, default_delay):
    """
    يحاول استخراج مدة الانتظار لو الـ API كتب:
    retry in 40.18s
    """
    match = re.search(
        r"retry(?:\s+in)?\s+([0-9]+(?:\.[0-9]+)?)\s*s",
        error_text,
        flags=re.IGNORECASE,
    )

    if match:
        try:
            return max(1, float(match.group(1)))
        except ValueError:
            pass

    return default_delay


def clean_model_name(name):
    """
    تنظيف اسم الموديل لحذف أي بادئة زائدة
    """
    if not name:
        return ""
    name = name.strip()
    if name.startswith("models/"):
        name = name[len("models/"):]
    return name


def ask_gemini(
    client,
    prompt,
    requested_model,
    fallback_model,
    max_retries=6,
    base_delay=5,
    status_slot=None,
):
    """
    استدعاء Gemini مع:
    - تنظيف اسم الموديل لعدم حدوث خطأ 404.
    - إعادة محاولة عند 429 / 503.
    - احترام مدة Retry لو ظهرت داخل رسالة الخطأ.
    - Fallback لو الموديل غير موجود.
    """

    req_cleaned = clean_model_name(requested_model)
    fall_cleaned = clean_model_name(fallback_model)

    current_model = req_cleaned if req_cleaned else fall_cleaned
    fallback = fall_cleaned if fall_cleaned else current_model

    last_exc = None

    for attempt in range(1, max_retries + 1):

        try:
            response = client.models.generate_content(
                model=current_model,
                contents=prompt,
            )

            text = getattr(response, "text", "") or ""

            if not text.strip():
                raise RuntimeError("تم استلام رد فارغ من الموديل.")

            return text

        except Exception as exc:

            last_exc = exc

            error_text = str(exc)
            error_lower = error_text.lower()

            not_found = (
                "404" in error_text
                or "not_found" in error_lower
                or "not found" in error_lower
                or "no longer available" in error_lower
            )

            overloaded = (
                "429" in error_text
                or "503" in error_text
                or "resource_exhausted" in error_lower
                or "unavailable" in error_lower
                or "overloaded" in error_lower
                or "high demand" in error_lower
                or "quota" in error_lower
            )

            # ------------------------------------------------
            # لو الموديل غير موجود
            # ------------------------------------------------
            if not_found and current_model != fallback:

                if status_slot is not None:
                    status_slot.warning(
                        f"⚠️ الموديل '{current_model}' غير متاح. "
                        f"جاري التجربة بالموديل البديل '{fallback}'."
                    )

                current_model = fallback
                continue

            # ------------------------------------------------
            # لو حصل Rate Limit أو ضغط
            # ------------------------------------------------
            if overloaded and attempt < max_retries:

                exponential_delay = base_delay * (2 ** (attempt - 1))

                retry_delay = extract_retry_delay(
                    error_text,
                    exponential_delay,
                )

                wait_time = max(retry_delay, exponential_delay)

                # سقف حتى لا ينتظر المستخدم وقتًا غير منطقي
                wait_time = min(wait_time, 120)

                if status_slot is not None:
                    status_slot.info(
                        f"⏳ Gemini طلب الانتظار بسبب ضغط أو حد استخدام. "
                        f"المحاولة {attempt}/{max_retries}. "
                        f"الانتظار حوالي {int(wait_time)} ثانية..."
                    )

                time.sleep(wait_time)
                continue

            raise

    raise last_exc


def build_prompt(
    mind,
    topic,
    audience_text,
    source_text,
    target_minutes,
    outputs,
):

    route_ids = ROUTES.get(mind["id"], [])

    selected_outputs = {}

    for mind_id in route_ids:
        key = f"العقل {mind_id}"

        if key in outputs and outputs[key].strip():
            selected_outputs[key] = outputs[key]

    project_context = COMMON_INPUTS.format(
        topic=topic,
        audience=audience_text,
        target_minutes=target_minutes,
        sources=source_text.strip()
        if source_text and source_text.strip()
        else "لا توجد مصادر إضافية قدمها المستخدم.",
    )

    return f"""
{GLOBAL_RULES}

{project_context}

================================================
اسم العقل:
{mind["name"]}

التخصص:
{mind["role"]}
================================================

تعليماتك الخاصة:

{mind["description"]}

================================================
مخرجات العقول السابقة المرتبطة بمهمتك
================================================

{format_context(selected_outputs)}

================================================
طريقة التعامل مع المخرجات السابقة
================================================

1. لا تعتبر أي مخرج سابق حقيقة مقدسة.
2. لو وجدت تكرارًا، لا تكرره.
3. لو وجدت تناقضًا، اختر القرار الأفضل واذكر السبب إذا كانت مهمتك تحتاج ذلك.
4. لا تحاول إظهار أنك قرأت كل شيء بتلخيصه مرة أخرى.
5. أضف قيمة جديدة واضحة.
6. لا تكتب كلامًا تنظيريًا للعقل التالي.
7. المخرج يجب أن يكون عمليًا وقابلًا للاستخدام.
8. التزم بمهمة عقلك فقط.
"""


# ============================================================
# Session State
# ============================================================

if "pipeline_outputs" not in st.session_state:
    st.session_state.pipeline_outputs = {}

if "pipeline_topic" not in st.session_state:
    st.session_state.pipeline_topic = ""

if "last_error" not in st.session_state:
    st.session_state.last_error = ""


# ============================================================
# إدخال الموضوع
# ============================================================

topic = st.text_area(
    "🎯 اكتب فكرة أو عنوان الفيديو",
    height=150,
    placeholder=(
        "مثال: ليه كل مرة ببدأ عادة جديدة وبعدين برجع لنقطة الصفر؟\n"
        "أو: الثقة بالنفس... ليه في ناس شكلهم واثقين رغم إنهم خايفين؟"
    ),
)


# ============================================================
# أزرار التحكم
# ============================================================

has_progress = len(st.session_state.pipeline_outputs) > 0

if has_progress:
    run_label = "▶️ كمّل من آخر عقل وقفنا عنده"
else:
    run_label = "🚀 شغّل نظام الـ10 عقول"

col_run, col_reset = st.columns([3, 1])

with col_run:
    run_button = st.button(
        run_label,
        type="primary",
        use_container_width=True,
    )

with col_reset:
    reset_button = st.button(
        "🔄 ابدأ من جديد",
        use_container_width=True,
    )


if reset_button:

    st.session_state.pipeline_outputs = {}
    st.session_state.pipeline_topic = ""
    st.session_state.last_error = ""

    st.rerun()


if has_progress:

    completed = len(st.session_state.pipeline_outputs)

    st.info(
        f"📌 تم حفظ {completed} من {len(MINDS)} عقول. "
        "لو حصل Rate Limit أو خطأ، تقدر تكمل بدون إعادة العقول اللي خلصت."
    )


# ============================================================
# التشغيل
# ============================================================

if run_button:

    if not api_key.strip():
        st.error("❌ اكتب Gemini API Key الأول.")
        st.stop()

    if not topic.strip():
        st.warning("⚠️ اكتب فكرة الفيديو أو العنوان الأول.")
        st.stop()

    # لو المستخدم غيّر الموضوع، امسح التقدم القديم
    if (
        st.session_state.pipeline_topic
        and st.session_state.pipeline_topic != topic.strip()
    ):

        st.warning(
            "⚠️ تم تغيير موضوع الفيديو، لذلك سيتم بدء Pipeline جديد."
        )

        st.session_state.pipeline_outputs = {}

    st.session_state.pipeline_topic = topic.strip()
    st.session_state.last_error = ""

    try:

        client = genai.Client(
            api_key=api_key.strip()
        )

        outputs = st.session_state.pipeline_outputs

        total_minds = len(MINDS)
        completed_count = len(outputs)

        progress = st.progress(
            completed_count / total_minds
            if completed_count
            else 0
        )

        status = st.empty()

        st.divider()
        st.subheader("🧠 غرفة الكتابة")

        st.caption(
            "كل عقل يستلم فقط السياق الذي يحتاجه. "
            "العقل 8 يكتب المسودة الكاملة، العقل 9 ينتقدها، "
            "والعقل 10 يعيد كتابتها كنسخة نهائية."
        )

        for index, mind in enumerate(MINDS):

            mind_key = f"العقل {mind['id']}"

            # --------------------------------------------
            # تخطي العقل المكتمل
            # --------------------------------------------
            if mind_key in outputs:

                if show_all_outputs:

                    with st.expander(
                        f"✅ {mind['name']} — محفوظ",
                        expanded=False,
                    ):
                        st.markdown(outputs[mind_key])

                progress.progress(
                    (index + 1) / total_minds
                )

                continue

            # --------------------------------------------
            # تشغيل العقل الحالي
            # --------------------------------------------
            status.info(
                f"🧠 العقل {mind['id']}/{total_minds} يعمل الآن: "
                f"{mind['name']}"
            )

            prompt = build_prompt(
                mind=mind,
                topic=topic.strip(),
                audience_text=audience,
                source_text=sources,
                target_minutes=target_minutes,
                outputs=outputs,
            )

            result = ask_gemini(
                client=client,
                prompt=prompt,
                requested_model=model_name,
                fallback_model=fallback_model,
                status_slot=status,
            )

            if not result.strip():
                raise RuntimeError(
                    f"العقل {mind['id']} أعاد نتيجة فارغة."
                )

            # --------------------------------------------
            # حفظ مباشر
            # --------------------------------------------
            outputs[mind_key] = result

            st.session_state.pipeline_outputs = outputs

            if show_all_outputs:

                with st.expander(
                    f"🧠 {mind['name']}",
                    expanded=(mind["id"] >= 8),
                ):
                    st.markdown(result)

            progress.progress(
                (index + 1) / total_minds
            )

        # ====================================================
        # السكريبت النهائي
        # ====================================================

        final_script = outputs.get(
            "العقل 10",
            ""
        )

        status.success(
            "🎉 اكتمل نظام الـ10 عقول والسكريبت النهائي جاهز."
        )

        st.divider()

        st.header("🎬 MASTER SCRIPT")

        st.text_area(
            "📜 النسخة النهائية",
            value=final_script,
            height=1000,
        )

        st.download_button(
            label="⬇️ تحميل السكريبت النهائي TXT",
            data=final_script,
            file_name="master_youtube_script.txt",
            mime="text/plain",
            use_container_width=True,
        )

        # ====================================================
        # تحميل جميع المخرجات
        # ====================================================

        all_outputs_text = []

        for mind in MINDS:

            key = f"العقل {mind['id']}"

            if key in outputs:

                all_outputs_text.append(
                    f"""
====================================================
{mind["name"]}
====================================================

{outputs[key]}
"""
                )

        st.download_button(
            label="⬇️ تحميل تقرير جميع العقول بالكامل TXT",
            data="\n\n".join(all_outputs_text),
            file_name="all_minds_report.txt",
            mime="text/plain",
            use_container_width=True,
        )

    except Exception as e:
        st.session_state.last_error = str(e)
        st.error(f"❌ حدث خطأ أثناء المعالجة: {str(e)}")

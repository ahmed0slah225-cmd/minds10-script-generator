# Minds10 — مولّد السيناريو

منصة ذكاء اصطناعي لإنتاج سكريبتات YouTube احترافية (طويلة، بالعامية المصرية).

## 🚀 التشغيل

```bash
pip install -r requirements.txt
export GEMINI_API_KEY="مفتاحك_من_Google_AI_Studio"
streamlit run app.py